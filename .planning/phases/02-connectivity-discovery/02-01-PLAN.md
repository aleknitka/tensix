---
phase: 02-connectivity-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/server/db/schema.ts, src/server/providers/types.ts, src/server/providers/ollama.ts, src/server/providers/openai.ts, src/server/index.ts, package.json]
autonomous: true
requirements: [CONN-01, CONN-02, CONN-03, CONN-04]

must_haves:
  truths:
    - "API returns health status for Ollama, LM Studio, and OpenRouter."
    - "API returns a list of available models for each configured provider."
    - "Model list includes capability flags (e.g., system_prompt_support)."
  artifacts:
    - path: "src/server/db/schema.ts"
      provides: "Persistence for provider settings"
      contains: "providerConfigs"
    - path: "src/server/providers/types.ts"
      provides: "Generic provider interface"
    - path: "src/server/index.ts"
      provides: "Endpoints for health and discovery"
      exports: ["GET /providers", "POST /providers", "GET /providers/models"]
  key_links:
    - from: "src/server/index.ts"
      to: "src/server/providers"
      via: "service call"
---

<objective>
Implement the backend foundation for connecting to LLM providers (Ollama, LM Studio, OpenRouter) and discovering model capabilities.

Purpose: Provide a robust API for the frontend to manage and verify connections.
Output: Persistent provider settings, provider adapters, and discovery endpoints.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/server/index.ts
@src/server/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and update DB schema</name>
  <files>package.json, src/server/db/schema.ts</files>
  <action>
    - Install `ollama`, `openai`, and `zod` packages using `npm install`.
    - Add `providerConfigs` table to `src/server/db/schema.ts` to store provider connection details (type, url, apiKey, name).
    - Run `npx drizzle-kit push` to update the SQLite database.
  </action>
  <verify>
    <automated>npm list ollama openai zod && sqlite3 sqlite.db ".schema provider_configs"</automated>
  </verify>
  <done>Dependencies installed and database schema updated.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Provider Adapters and Health Checks</name>
  <files>src/server/providers/types.ts, src/server/providers/ollama.ts, src/server/providers/openai.ts</files>
  <action>
    - Define a generic `LLMProvider` interface in `src/server/providers/types.ts`.
    - Implement `OllamaProvider` using `ollama-js`.
    - Implement `OpenAICompatibleProvider` using `openai` (for LM Studio and OpenRouter).
    - Each provider must implement a `checkHealth()` method and a `listModels()` method.
    - Implement capability detection in `listModels()` by checking model tags or running a small probe request (for CONN-04).
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create test/provider.test.ts first</automated>
  </verify>
  <done>Ollama and OpenAI-compatible adapters implemented with health check and model discovery logic.</done>
</task>

<task type="auto">
  <name>Task 3: Create API endpoints for Providers</name>
  <files>src/server/index.ts</files>
  <action>
    - Add Hono routes:
        - `GET /providers`: List all configured providers.
        - `POST /providers`: Add or update a provider configuration.
        - `GET /providers/:id/health`: Run health check for a specific provider.
        - `GET /providers/:id/models`: Fetch dynamically updated list of models for a provider.
    - Ensure routes use the persistence layer and provider adapters.
  </action>
  <verify>
    <automated>curl http://localhost:3001/providers</automated>
  </verify>
  <done>Hono endpoints for provider management and discovery are active.</done>
</task>

</tasks>

<verification>
Check if the API correctly responds with provider health and model lists.
</verification>

<success_criteria>
1. Backend can persist and retrieve provider configurations.
2. API provides real-time health status for local and cloud LLM providers.
3. API returns a dynamically updated list of available models with capability flags.
</success_criteria>

<output>
After completion, create `.planning/phases/02-connectivity-discovery/02-01-SUMMARY.md`
</output>
