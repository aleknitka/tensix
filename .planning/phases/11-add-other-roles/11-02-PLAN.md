---
phase: 11-add-other-roles
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified: [roles/**/*]
autonomous: true
requirements: [ROLE-CONTENT]
must_haves:
  truths:
    - "Role files are organized into subfolders (six-hats, scamper)"
    - "Original six hats are moved to roles/six-hats/"
    - "At least 7 new SCAMPER roles exist"
    - "New roles have chattiness_limit set to 3-4 sentences"
  artifacts:
    - path: "roles/six-hats/"
      provides: "Original Six Thinking Hats"
    - path: "roles/scamper/"
      provides: "SCAMPER framework roles"
  key_links:
    - from: "roles/scamper/substitute.yml"
      to: "scamper-substitute"
      via: "ID matching"
---

<objective>
Refactor the role directory structure and implement the SCAMPER framework roles. This begins the expansion of the expert template library.

Purpose: Provide users with the SCAMPER cognitive framework and establish the hierarchical directory structure.
Output: Reorganized `roles/` directory and 7 new SCAMPER templates.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-add-other-roles/11-CONTEXT.md
@.planning/phases/11-add-other-roles/11-RESEARCH.md
@.planning/phases/11-add-other-roles/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Role Directory Structure</name>
  <files>roles/**/*</files>
  <action>
    - Create subdirectories in `roles/`: `six-hats/`, `scamper/`, `business/`, `technical/`, `creative/` (per D-01).
    - Move original 6 hats (black, blue, green, red, white, yellow) into `roles/six-hats/`.
    - Ensure the move is reflected in the file system.
  </action>
  <verify>
    <automated>ls roles/six-hats/black-hat.yml</automated>
  </verify>
  <done>Directory structure reorganized for hierarchical storage.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SCAMPER Framework Roles</name>
  <files>roles/scamper/*.yml</files>
  <action>
    - Create 7 new role files in `roles/scamper/` based on the research content:
      - `substitute.yml`, `combine.yml`, `adapt.yml`, `modify.yml`, `repurpose.yml`, `eliminate.yml`, `reverse.yml`.
    - Each file must include: `id`, `name`, `role_type` ('critic'), `description`, `systemPrompt`, `chattiness_limit: 4`, `icon_id`, and `color_accent` as defined in `11-RESEARCH.md`.
    - Include specific "conciseness" instructions in the `systemPrompt` for each.
  </action>
  <verify>
    <automated>ls roles/scamper/ | wc -l | grep 7</automated>
  </verify>
  <done>SCAMPER framework templates implemented.</done>
</task>

</tasks>

<verification>
Run the application and check the server logs (or run Task 2 of Plan 1) to ensure the new roles are correctly synced into the database with their respective categories.
</verification>

<success_criteria>
- [ ] Roles are organized into subdirectories.
- [ ] Original six hats are in roles/six-hats/.
- [ ] 7 new SCAMPER YAML role templates are added.
- [ ] All new roles have anti-chattiness constraints.
</success_criteria>

<output>
After completion, create `.planning/phases/11-add-other-roles/11-02-SUMMARY.md`
</output>
