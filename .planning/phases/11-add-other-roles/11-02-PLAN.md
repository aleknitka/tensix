---
phase: 11-add-other-roles
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified: [roles/**/*]
autonomous: true
requirements: [ROLE-CONTENT]
must_haves:
  truths:
    - "Role files are organized into subfolders (scamper, business, etc.)"
    - "At least 15 new roles exist across the new categories"
    - "New roles have chattiness_limit set to 3-4 sentences"
  artifacts:
    - path: "roles/six-hats/"
      provides: "Original Six Thinking Hats"
    - path: "roles/scamper/"
      provides: "SCAMPER framework roles"
    - path: "roles/business/"
      provides: "Specialized business personas"
  key_links:
    - from: "roles/scamper/substitute.yml"
      to: "scamper-substitute"
      via: "ID matching"
---

<objective>
Expand the expert template library with multiple cognitive frameworks and specialized personas, organized in a hierarchical structure. This populates the system with the 15+ new templates identified in research.

Purpose: Provide users with a diverse set of reasoning tools beyond the initial Six Thinking Hats.
Output: Reorganized `roles/` directory with 20+ total templates (6 original + 15+ new).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-add-other-roles/11-CONTEXT.md
@.planning/phases/11-add-other-roles/11-RESEARCH.md
@.planning/phases/11-add-other-roles/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Role Directory Structure</name>
  <files>roles/**/*</files>
  <action>
    - Create subdirectories in `roles/`: `six-hats/`, `scamper/`, `business/`, `technical/`, `creative/` (per D-01).
    - Move original 6 hats (black, blue, green, red, white, yellow) into `roles/six-hats/`.
    - Ensure the move is reflected in the file system.
  </action>
  <verify>
    <automated>ls roles/six-hats/black-hat.yml</automated>
  </verify>
  <done>Directory structure reorganized for hierarchical storage.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SCAMPER Framework Roles</name>
  <files>roles/scamper/*.yml</files>
  <action>
    - Create 7 new role files in `roles/scamper/` based on the research content:
      - `scamper-substitute.yml`, `scamper-combine.yml`, `scamper-adapt.yml`, `scamper-modify.yml`, `scamper-repurpose.yml`, `scamper-eliminate.yml`, `scamper-reverse.yml`.
    - Each file must include: `id`, `name`, `role_type` ('critic'), `description`, `systemPrompt`, `chattiness_limit: 4`, `icon_id`, and `color_accent` as defined in `11-RESEARCH.md`.
    - Include specific "conciseness" instructions in the `systemPrompt` for each.
  </action>
  <verify>
    <automated>ls roles/scamper/ | wc -l | grep 7</automated>
  </verify>
  <done>SCAMPER framework templates implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Domain-Specific Roles</name>
  <files>roles/business/*.yml, roles/technical/*.yml, roles/creative/*.yml</files>
  <action>
    - Create the following roles in their respective folders as defined in `11-RESEARCH.md`:
      - `business/`: Customer Advocate, Competitor Analyst, CFO, Project Manager.
      - `technical/`: Security Auditor, Scalability Expert, UI/UX Designer.
      - `creative/`: Devil's Advocate, The Minimalist, The Futurist.
    - Each must follow the conciseness constraints and include Phase 9 metadata (icons/colors).
    - Ensure all roles have a default `chattiness_limit` of 3-4 (per D-04).
  </action>
  <verify>
    <automated>ls roles/business/ roles/technical/ roles/creative/ | wc -l | grep 10</automated>
  </verify>
  <done>Specialized domain personas implemented across Business, Tech, and Creative.</done>
</task>

</tasks>

<verification>
Run the application and check the server logs (or run Task 2 of Plan 1) to ensure the new roles are correctly synced into the database with their respective categories.
</verification>

<success_criteria>
- [ ] Roles are organized into 5 subdirectories.
- [ ] 17+ new YAML role templates are added.
- [ ] All new roles have anti-chattiness constraints.
- [ ] Original roles function correctly in their new location.
</success_criteria>

<output>
After completion, create `.planning/phases/11-add-other-roles/11-02-SUMMARY.md`
</output>
