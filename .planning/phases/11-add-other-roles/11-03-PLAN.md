---
phase: 11-add-other-roles
plan: 03
type: execute
wave: 3
depends_on: [11-01]
files_modified: [src/components/RoleSelector.tsx]
autonomous: true
requirements: [ROLE-UI]
must_haves:
  truths:
    - "RoleSelector displays roles grouped by category"
    - "Category sections are collapsible"
    - "Search filters across all categories and expands those with matches"
  artifacts:
    - path: "src/components/RoleSelector.tsx"
      provides: "Hierarchical UI for role selection"
      contains: "Object.entries(grouped)"
  key_links:
    - from: "src/components/RoleSelector.tsx"
      to: "persona.category"
      via: "Grouping logic"
---

<objective>
Update the RoleSelector UI to support hierarchical selection with categories and collapsible sections. This makes the expanded role library manageable and easy to navigate.

Purpose: Improve discoverability of 20+ roles while preventing UI clutter.
Output: Hierarchical, searchable, and collapsible `RoleSelector` component.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-add-other-roles/11-CONTEXT.md
@.planning/phases/11-add-other-roles/11-RESEARCH.md
@.planning/phases/11-add-other-roles/11-01-SUMMARY.md
@src/components/RoleSelector.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update RoleSelector Logic and Grouping</name>
  <files>src/components/RoleSelector.tsx</files>
  <action>
    - Update the `Persona` interface to include the `category` field.
    - Implement a grouping function that takes the filtered personas and groups them by their `category` field.
    - Handle the 'general' or 'six-hats' category naming (per research recommendation).
    - Update the search filter to work across name, role, description, and category.
  </action>
  <verify>
    <automated>npm test tests/components/RoleSelector.test.ts</automated>
  </verify>
  <done>Logic for grouping and filtering by category implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Hierarchical UI with Collapsible Sections</name>
  <files>src/components/RoleSelector.tsx</files>
  <action>
    - Refactor the render logic in `RoleSelector.tsx`:
      - Iterate over the grouped categories.
      - For each category, render a collapsible header (using local state to track open/closed status).
      - Render the role cards within each category section.
      - Automatically expand categories that contain matching roles when searching (per research discretion).
    - Update role card visuals:
      - Use `persona.icon_id` with `Lucide React` for dynamic icons (instead of the hardcoded switch).
      - Use `persona.color_accent` for branding highlights.
  </action>
  <verify>
    <automated>npm test tests/components/RoleSelector.test.ts</automated>
  </verify>
  <done>UI displays grouped, collapsible categories with updated role card visuals.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Hierarchical Role Selector</what-built>
  <how-to-verify>
    1. Launch the application.
    2. Open a session and click to add a persona.
    3. Verify that roles are grouped by categories (e.g., 'scamper', 'business').
    4. Click category headers to expand/collapse them.
    5. Type in the search box (e.g., 'security') and verify the 'technical' category auto-expands to show the 'Security Auditor' role.
    6. Ensure icons and colors match the new role definitions.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Human verify the visual hierarchy and interaction behavior in the UI.
</verification>

<success_criteria>
- [ ] Roles are grouped by category in the UI.
- [ ] Categories are collapsible.
- [ ] Search works across all categories.
- [ ] Role cards display custom icons and colors.
</success_criteria>

<output>
After completion, create `.planning/phases/11-add-other-roles/11-03-SUMMARY.md`
</output>
