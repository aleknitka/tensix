---
phase: 11-add-other-roles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/server/db/schema.ts, src/server/services/role-schema.ts, src/server/services/role-sync.ts, tests/role-sync.test.ts, tests/roles.test.ts]
autonomous: true
requirements: [ROLE-SCAN, ROLE-SCHEMA]
must_haves:
  truths:
    - "Sync service extracts category from directory name (e.g., 'scamper/')"
    - "Role files with Phase 9 parameters (temperature, max_tokens, etc.) are correctly ingested into the database"
    - "Database schema includes 'category' column for personas"
  artifacts:
    - path: "src/server/db/schema.ts"
      provides: "Updated database schema with category field"
      contains: "category: text('category')"
    - path: "src/server/services/role-sync.ts"
      provides: "Recursive file scanning and category assignment"
      contains: "fast-glob"
    - path: "tests/role-sync.test.ts"
      provides: "Verification of recursive scanning"
  key_links:
    - from: "src/server/services/role-sync.ts"
      to: "path.dirname(file)"
      via: "Category extraction"
---

<objective>
Update the database schema and sync service to support hierarchical role organization and the full suite of Phase 9 persona fields. This ensures the foundation is ready for the 15+ new templates and the hierarchical UI.

Purpose: Enable organizational scaling of the role library and feature parity between YAML templates and UI-created personas.
Output: Enhanced `personas` table, upgraded `syncRoles` service, and Wave 0 verification tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-add-other-roles/11-CONTEXT.md
@.planning/phases/11-add-other-roles/11-RESEARCH.md
@src/server/db/schema.ts
@src/server/services/role-schema.ts
@src/server/services/role-sync.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Database Schema and Validation</name>
  <files>src/server/db/schema.ts, src/server/services/role-schema.ts</files>
  <action>
    - Update `src/server/db/schema.ts`: Add `category: text('category').default('general')` to the `personas` table.
    - Update `src/server/services/role-schema.ts`: Expand `roleSchema` to include optional Phase 9 fields: `icon_id` (string), `color_accent` (string), `skills` (string array), and `parameters` (object containing temperature, top_p, max_tokens, presence_penalty, frequency_penalty as optional numbers).
    - Ensure `role_type` enum in `roleSchema` includes 'critic' as per research.
  </action>
  <verify>
    <automated>npm test tests/roles.test.ts</automated>
  </verify>
  <done>Schema and validation updated to support Phase 9 and categories.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Role Sync Service</name>
  <files>src/server/services/role-sync.ts</files>
  <action>
    - Update `syncRoles` in `src/server/services/role-sync.ts`:
      - Change glob pattern to `**/*.yml` to support subdirectories.
      - Calculate `category` from folder name using `path.dirname(file)`. If dirname is '.', default to 'general' or 'six-hats' as appropriate (per D-01).
      - Update the upsert logic to include `category`, `icon_id`, `color_accent`, `skills` (JSON stringified), and all model `parameters` (temperature, top_p, etc.).
    - Ensure `chattiness_limit` is correctly synced (as per D-04).
  </action>
  <verify>
    <automated>npm run test tests/role-sync.test.ts</automated>
  </verify>
  <done>Sync service scans recursively and maps directory names to categories.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 - Create Sync Tests</name>
  <files>tests/role-sync.test.ts</files>
  <action>
    - Create `tests/role-sync.test.ts` to mock the file system (or use a temp directory) and verify:
      - Roles in the root are assigned to the default category.
      - Roles in `six-hats/` are assigned to 'six-hats'.
      - Roles in `scamper/` are assigned to 'scamper'.
      - All Phase 9 fields are correctly mapped from YAML to database columns.
  </action>
  <verify>
    <automated>npm run test tests/role-sync.test.ts</automated>
  </verify>
  <done>Tests verify sync logic robustness and category extraction.</done>
</task>

</tasks>

<verification>
Check that the database reflects the new column and that the sync service handles nested directories.
</verification>

<success_criteria>
- [ ] Database `personas` table has `category` column.
- [ ] `syncRoles` service uses recursive globbing.
- [ ] Roles in folders are assigned correct categories.
- [ ] All Phase 9 fields (icons, colors, parameters) are successfully synced from YAML.
</success_criteria>

<output>
After completion, create `.planning/phases/11-add-other-roles/11-01-SUMMARY.md`
</output>
