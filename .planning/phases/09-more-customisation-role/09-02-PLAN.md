---
phase: 09-more-customisation-role
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: [src/components/PersonaEditor.tsx, src/components/IconPicker.tsx, src/components/ColorPicker.tsx]
autonomous: true
requirements: [UI-07, UI-08]
must_haves:
  truths:
    - "Users can adjust model parameters via sliders/inputs in the editor"
    - "Sliders support 'Provider Default' state for null values"
    - "IconPicker and ColorPicker allow selection of branding identity"
    - "All new parameters and metadata are saved when updating a persona"
  artifacts:
    - path: "src/components/PersonaEditor.tsx"
      provides: "New UI sections for parameters and branding"
    - path: "src/components/IconPicker.tsx"
      provides: "Grid of Lucide icons for persona selection"
    - path: "src/components/ColorPicker.tsx"
      provides: "Grid of color accents for persona selection"
  key_links:
    - from: "src/components/PersonaEditor.tsx"
      to: "Backend PUT /personas/:id"
      via: "form submission"
      pattern: "fetch.*/api/personas/.*method: 'PUT'"
---

<objective>
Enhance the persona editor with granular controls for model parameters and a visual branding system.

Purpose: Allow users to customize persona behavior and appearance.
Output: Updated PersonaEditor with parameter sliders and branding pickers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-more-customisation-role/09-CONTEXT.md
@.planning/phases/09-more-customisation-role/09-RESEARCH.md
@.planning/phases/09-more-customisation-role/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model Parameter Controls</name>
  <files>src/components/PersonaEditor.tsx</files>
  <action>
    Add a "Model Parameters" section to `PersonaEditor.tsx` (per D-01):
    - Sliders for `temperature` (0.0 to 2.0), `top_p` (0.0 to 1.0), `presence_penalty` (-2.0 to 2.0), and `frequency_penalty` (-2.0 to 2.0).
    - Number input for `max_tokens`.
    - Handle null/undefined values by showing "Provider Default" state.
    - Wire to persona form state and Ensure they are saved on "Save".
  </action>
  <verify>
    <automated>grep -E "temperature|top_p|presence_penalty" src/components/PersonaEditor.tsx</automated>
  </verify>
  <done>Parameter sliders are available and functional in the editor.</done>
</task>

<task type="auto">
  <name>Task 2: Visual Branding Components</name>
  <files>src/components/IconPicker.tsx, src/components/ColorPicker.tsx</files>
  <action>
    1. Create `IconPicker.tsx`: A grid of the 16 Lucide icons defined in RESEARCH.md. Allow selection by `icon_id`.
    2. Create `ColorPicker.tsx`: A grid of the 8 Tailwind color accents defined in RESEARCH.md. Allow selection by `color_accent`.
    3. Both should be accessible and visually clear.
  </action>
  <verify>
    <automated>ls src/components/IconPicker.tsx src/components/ColorPicker.tsx</automated>
  </verify>
  <done>IconPicker and ColorPicker components are created.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Branding into Editor</name>
  <files>src/components/PersonaEditor.tsx</files>
  <action>
    1. Add `IconPicker` and `ColorPicker` to a "Visual Identity" section in `PersonaEditor.tsx` (per D-03).
    2. Add a tag-style input for `skills` (JSON array) (per D-04).
    3. Ensure all branding fields are saved on form submission.
  </action>
  <verify>
    <automated>grep -E "IconPicker|ColorPicker|skills" src/components/PersonaEditor.tsx</automated>
  </verify>
  <done>Visual branding and skills metadata integrated into the editor.</done>
</task>

</tasks>

<verification>
- Model parameter sliders work correctly and update state.
- IconPicker and ColorPicker allow selection of branding assets.
- New fields are saved to the backend correctly.
</verification>

<success_criteria>
- Personas can be assigned custom icons and color accents.
- New metadata (skills, params) is correctly saved and persists across sessions.
</success_criteria>

<output>
After completion, create `.planning/phases/09-more-customisation-role/09-02-SUMMARY.md`
</output>
