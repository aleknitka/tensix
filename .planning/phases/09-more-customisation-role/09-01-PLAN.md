---
phase: 09-more-customisation-role
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/server/db/schema.ts, src/server/providers/types.ts, src/server/providers/ollama.ts, src/server/providers/lmstudio.ts, src/server/providers/openrouter.ts, src/server/index.ts]
autonomous: true
requirements: [PERS-04, PERS-05, API-03]
---

<objective>
Update the persona storage schema and backend providers to support advanced model parameters and visual identity metadata. Implement a stateless sandbox endpoint for persona testing.

Purpose: Provide the technical foundation for granular model control and persona branding.
Output: Updated database schema, enhanced provider adapters, and a new test API endpoint.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-more-customisation-role/09-CONTEXT.md
@.planning/phases/09-more-customisation-role/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Database Schema</name>
  <files>src/server/db/schema.ts</files>
  <action>
    Add the following columns to the `personas` table in `src/server/db/schema.ts` (per D-01, D-03, D-04):
    - `temperature` (real)
    - `top_p` (real)
    - `max_tokens` (integer)
    - `presence_penalty` (real)
    - `frequency_penalty` (real)
    - `icon_id` (text, default 'user-circle')
    - `color_accent` (text, default 'slate')
    - `skills` (text, default '[]' for JSON array)
    
    Run `npx drizzle-kit generate` and `npx drizzle-kit push` (or equivalent migration command) to update the SQLite database.
  </action>
  <verify>
    <automated>sqlite3 sqlite.db ".schema personas" | grep -E "temperature|icon_id|skills"</automated>
  </verify>
  <done>Database schema updated with new persona columns.</done>
</task>

<task type="auto">
  <name>Task 2: Update Provider Adapters</name>
  <files>src/server/providers/types.ts, src/server/providers/ollama.ts, src/server/providers/lmstudio.ts, src/server/providers/openrouter.ts</files>
  <action>
    1. Update `BaseProvider` in `types.ts` to accept an optional `options` parameter in the `generate` method:
       `abstract generate(modelId: string, messages: Message[], tools?: any[], options?: ModelOptions): AsyncIterable<GenerateChunk>;`
       Define `ModelOptions` interface with temperature, top_p, max_tokens, etc.
    2. Implement parameter mapping in `OllamaProvider` (mapping `max_tokens` to `options.num_predict` per Research Pitfall 1).
    3. Implement parameter mapping in `LMStudioProvider` and `OpenRouterProvider` (standard OpenAI-compatible params).
  </action>
  <verify>
    <automated>npm run test tests/orchestrator.test.ts</automated>
  </verify>
  <done>Adapters successfully pass advanced parameters to LLM providers.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Sandbox API Endpoint</name>
  <files>src/server/index.ts</files>
  <action>
    Create a `POST /personas/test` endpoint in `src/server/index.ts` (per D-02):
    - Accepts: `{ systemPrompt, modelId, providerId, parameters, testPrompt }`
    - Logic:
      1. Get the adapter for `providerId`.
      2. Construct message list: `[{ role: 'system', content: systemPrompt }, { role: 'user', content: testPrompt }]`.
      3. Call `adapter.generate(modelId, messages, [], parameters)`.
      4. Stream the results back using `hono/streaming`.
    - Constraint: Do NOT save any messages or update sessions in this endpoint.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:3001/personas/test -H "Content-Type: application/json" -d '{"testPrompt": "hi"}'</automated>
  </verify>
  <done>Stateless sandbox endpoint is functional and streaming.</done>
</task>

</tasks>

<verification>
- Database schema matches RESEARCH.md requirements.
- Provider adapters correctly map parameters (especially Ollama `num_predict`).
- Sandbox endpoint streams response without side effects.
</verification>

<success_criteria>
- Users can configure temperature and other model-specific parameters in the DB.
- New metadata (skills, params) is correctly saved and persists across sessions.
</success_criteria>

<output>
After completion, create `.planning/phases/09-more-customisation-role/09-01-SUMMARY.md`
</output>
