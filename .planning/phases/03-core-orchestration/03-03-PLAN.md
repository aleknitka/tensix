---
phase: 03-core-orchestration
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [src/app/sessions/[id]/page.tsx, src/components/DebateView.tsx, src/components/RoundTableConfig.tsx]
autonomous: false
requirements: [PERS-03, UI-01]

must_haves:
  truths:
    - "User can assign models to different 'Hats' before starting a debate"
    - "Debate view shows messages streaming in real-time"
    - "UI provides visual feedback on which expert is currently 'thinking'"
  artifacts:
    - path: "src/app/sessions/[id]/page.tsx"
      provides: "Session-specific debate page"
    - path: "src/components/DebateView.tsx"
      provides: "SSE-consuming streaming component"
  key_links:
    - from: "src/components/DebateView.tsx"
      to: "/api/sessions/:id/round-table"
      via: "EventSource for SSE"
---

<objective>
Complete the Round Table feature by building the frontend UI and integrating the streaming backend.

Purpose: To provide the end-user with an interactive and informative experience during multi-persona evaluations.
Output: Real-time debate view and configuration components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/app/page.tsx
@src/components/ModelSelector.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Round Table Configuration UI</name>
  <files>src/components/RoundTableConfig.tsx</files>
  <action>
    Create `RoundTableConfig.tsx` component that:
    1. Fetches the list of personas (6 Thinking Hats).
    2. Allows users to toggle which hats to include in the round table.
    3. Uses `ModelSelector` to let the user assign a specific model to each selected hat.
    4. Has a "Start Debate" button that sends the configuration to the backend.
  </action>
  <verify>
    Component renders personas and model selectors.
  </verify>
  <done>Configuration UI allows per-persona model selection.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Real-time Debate View</name>
  <files>src/components/DebateView.tsx, src/app/sessions/[id]/page.tsx</files>
  <action>
    Create `DebateView.tsx` component that:
    1. Consumes the `/api/sessions/:id/round-table` SSE endpoint.
    2. Displays a vertical list of messages.
    3. Shows a "typing" or "thinking" state for the current active expert.
    4. Updates the message content in real-time as tokens arrive.
    5. Correctly handles multiple consecutive agent responses.
    
    Implement `src/app/sessions/[id]/page.tsx` as the container for this view, fetching initial session history.
  </action>
  <verify>
    User can start a debate and see text appear as it's generated by different models.
  </verify>
  <done>Debate view correctly streams and displays the round-table session.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Phase 3 features: Serial Expert Queue with Six Thinking Hats personas streaming to the UI.</what-built>
  <how-to-verify>
    1. Start the application (`npm run dev` and `npm run dev:backend`).
    2. Create a New Session.
    3. Open the session and configure a round table with 2-3 hats (e.g., White and Black).
    4. Assign models to them.
    5. Click "Start Debate".
    6. Verify that each hat takes turns and their output streams in real-time.
    7. Check backend logs to ensure models are loaded/unloaded sequentially.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Manually verify the full end-to-end flow of starting a session, configuring hats, and observing the sequential streaming debate.
</verification>

<success_criteria>
- PERS-03: User can assign models to personas.
- UI-01: Debate view streams in real-time.
- VRAM usage stays within limits due to sequential execution (ORCH-01).
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-orchestration/03-03-SUMMARY.md`
</output>
