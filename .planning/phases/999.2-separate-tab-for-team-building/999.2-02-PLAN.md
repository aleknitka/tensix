---
phase: 999.2-separate-tab-for-team-building
plan: 02
type: execute
wave: 2
depends_on: [999.2-01]
files_modified: [src/app/sessions/page.tsx, src/components/RoundTableConfig.tsx, src/components/SessionSettings.tsx, src/components/ToolsTab.tsx]
autonomous: true
requirements: [999.2-TAB-03, 999.2-TAB-04]
must_haves:
  truths:
    - "Export buttons (JSON, Markdown, Audit Report) are moved from the session header to the Settings tab."
    - "The Settings tab contains orchestration mode and max turns controls."
    - "The Tools tab displays a placeholder or list of active tools."
    - "RoundTableConfig is refactored to focus solely on persona selection."
  artifacts:
    - path: "src/components/SessionSettings.tsx"
      provides: "Consolidated session settings and export actions"
    - path: "src/components/ToolsTab.tsx"
      provides: "Visibility into active tools"
---

<objective>
Complete the content migration by implementing the Settings and Tools tabs, and refactoring RoundTableConfig to follow the UI spec.

Purpose: Declutter the interface and group related functionality logically.
Output: Fully functional tabbed sidebar with migrated header actions and refactored components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.2-separate-tab-for-team-building/999.2-CONTEXT.md
@.planning/phases/999.2-separate-tab-for-team-building/999.2-UI-SPEC.md
@.planning/phases/999.2-separate-tab-for-team-building/999.2-01-SUMMARY.md
@src/app/sessions/page.tsx
@src/components/RoundTableConfig.tsx

<interfaces>
From src/app/sessions/page.tsx (after Plan 01):
```typescript
const [mode, setMode] = useState<'sequential' | 'auto' | 'hitl'>('sequential');
const [maxTurns, setMaxTurns] = useState(10);
const handleExportJson = () => { ... };
const handleExportMarkdown = () => { ... };
const handleGenerateReport = () => { ... };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SessionSettings Component & Migrate Actions</name>
  <files>src/components/SessionSettings.tsx, src/app/sessions/page.tsx</files>
  <action>
    - Create `SessionSettings.tsx` to include:
      - Export buttons (migrated from `SessionPage` header).
      - Orchestration configuration (Mode selection and Max Turns slider, extracted from `RoundTableConfig`).
    - Use props for the export handlers and orchestration state/handlers.
    - In `src/app/sessions/page.tsx`, remove the export buttons from the `<header>` and integrate `SessionSettings` into the `settings` tab.
  </action>
  <verify>
    <automated>grep "SessionSettings" src/app/sessions/page.tsx</automated>
  </verify>
  <done>Settings tab implemented with export actions and orchestration config.</done>
</task>

<task type="auto">
  <name>Task 2: Update RoundTableConfig & Move Insights</name>
  <files>src/components/RoundTableConfig.tsx, src/app/sessions/page.tsx</files>
  <action>
    - In `src/components/RoundTableConfig.tsx`, remove the grid containing Orchestration Mode and Max Turns.
    - In `src/app/sessions/page.tsx`, move the "Session Insights" card from the sidebar to be a header within the "Team" tab view (just above `RoundTableConfig`).
    - Redesign `RoundTableConfig` to fit the full-width tab layout better (remove redundant inner shadow/border if necessary).
  </action>
  <verify>
    <automated>grep -v "maxTurns" src/components/RoundTableConfig.tsx</automated>
  </verify>
  <done>RoundTableConfig refactored to focus on participant selection.</done>
</task>

<task type="auto">
  <name>Task 3: Create ToolsTab Component & Final UI Polish</name>
  <files>src/components/ToolsTab.tsx, src/app/sessions/page.tsx</files>
  <action>
    - Create `ToolsTab.tsx` as a simple list component showing tools (e.g., `read_file`, `calculator`).
    - Integrate it into the `tools` tab in `SessionPage`.
    - Apply final styling to all sidebar tabs (padding, borders, transitions) according to the spacing and color standards in `999.2-UI-SPEC.md`.
    - Update `RoundTableConfig` to auto-switch to the 'chat' tab when an evaluation is started (call `onStart` callback in `SessionPage`).
  </action>
  <verify>
    <automated>ls src/components/ToolsTab.tsx</automated>
  </verify>
  <done>Tools tab created and global sidebar UI polished.</done>
</task>

</tasks>

<verification>
Manual verification: Open a session, navigate to Settings, verify export buttons work. Navigate to Team, verify Start Audit works and switches back to Discussion (Chat). Verify the header is cleaner without export buttons.
</verification>

<success_criteria>
- All 5 tabs fully functional.
- Session header simplified.
- Orchestration config moved to Settings.
- Tools tab active.
- Auto-tab-switch on Start Evaluation.
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-separate-tab-for-team-building/999.2-02-SUMMARY.md`
</output>
