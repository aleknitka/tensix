---
phase: 999.2-separate-tab-for-team-building
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/sessions/page.tsx, src/components/RoundTableConfig.tsx, src/components/SidebarTabs.tsx]
autonomous: true
requirements: [999.2-TAB-01, 999.2-TAB-02]
must_haves:
  truths:
    - "User can see a tab bar at the top of the right sidebar in the session view."
    - "Clicking different tabs switches the content of the right sidebar."
    - "Orchestration settings (mode, max turns) still function correctly after being lifted."
  artifacts:
    - path: "src/components/SidebarTabs.tsx"
      provides: "Tab navigation for the sidebar"
    - path: "src/app/sessions/page.tsx"
      provides: "Tab state management and conditional rendering"
  key_links:
    - from: "src/components/SidebarTabs.tsx"
      to: "src/app/sessions/page.tsx"
      via: "setActiveTab callback"
---

<objective>
Establish the foundation for the tabbed sidebar by implementing the tab navigation component, lifting orchestration state, and setting up conditional rendering in the session page.

Purpose: Clearer separation of concerns and improved readability for the chat history.
Output: Working tab switcher in the session sidebar with basic state management.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.2-separate-tab-for-team-building/999.2-CONTEXT.md
@.planning/phases/999.2-separate-tab-for-team-building/999.2-UI-SPEC.md
@src/app/sessions/page.tsx
@src/components/RoundTableConfig.tsx

<interfaces>
From src/components/RoundTableConfig.tsx:
```typescript
interface RoundTableConfigProps {
  sessionId: string;
  onStartEvaluation: (personaIds: string[], mode: string, maxTurns: number) => void;
  onStopEvaluation: () => void;
  isEvaluating: boolean;
  // New props to be added:
  mode: 'sequential' | 'auto' | 'hitl';
  onModeChange: (mode: 'sequential' | 'auto' | 'hitl') => void;
  maxTurns: number;
  onMaxTurnsChange: (turns: number) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift Orchestration State to SessionContent</name>
  <files>src/app/sessions/page.tsx, src/components/RoundTableConfig.tsx</files>
  <action>
    - In `src/app/sessions/page.tsx`, add `mode` and `maxTurns` state to the `SessionContent` component (default to 'sequential' and 10).
    - Update `handleStartEvaluation` to use these states instead of receiving them as arguments (or make them optional).
    - In `src/components/RoundTableConfig.tsx`, remove the local `mode` and `maxTurns` state and use props instead.
    - Pass the state and change handlers from `SessionContent` to `RoundTableConfig`.
  </action>
  <verify>
    <automated>npm test -- tests/orchestrator.test.ts</automated>
  </verify>
  <done>Orchestration settings are managed by the session page and passed to the config component.</done>
</task>

<task type="auto">
  <name>Task 2: Create SidebarTabs Component</name>
  <files>src/components/SidebarTabs.tsx</files>
  <action>
    - Create a new component `SidebarTabs` that renders a horizontal list of icons with tooltips or labels.
    - Icons (from lucide-react): `Users` (Team), `FileText` (Docs), `GitBranch` (Branches), `Settings` (Settings), `Wrench` (Tools).
    - Implement a simple active state styling (e.g., darker background/border for active tab per UI-SPEC).
    - Accept `activeTab` and `onTabChange` as props.
  </action>
  <verify>
    <automated>ls src/components/SidebarTabs.tsx</automated>
  </verify>
  <done>SidebarTabs component created with correct icons and props.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Tab Switching in SessionPage</name>
  <files>src/app/sessions/page.tsx</files>
  <action>
    - In `SessionContent`, add `activeTab` state (default 'team').
    - Import and place `SidebarTabs` at the top of the right sidebar container.
    - Wrap the existing sidebar components (`BranchNavigator`, `DocumentManager`, `RoundTableConfig`) in conditional logic based on `activeTab`.
    - For now, map 'team' to `RoundTableConfig` and "Session Insights", 'docs' to `DocumentManager`, and 'branches' to `BranchNavigator`. 'settings' and 'tools' can be placeholders.
  </action>
  <verify>
    <automated>grep "activeTab" src/app/sessions/page.tsx</automated>
  </verify>
  <done>Session sidebar content is now switchable via tabs.</done>
</task>

</tasks>

<verification>
Manual verification: Open a session, click through the sidebar tabs, and ensure the correct components appear. Start an evaluation and verify it still works with the lifted state.
</verification>

<success_criteria>
- Sidebar has 5 functional tabs.
- RoundTableConfig, DocumentManager, and BranchNavigator are correctly distributed across tabs.
- Evaluation still works with lifted mode/turns state.
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-separate-tab-for-team-building/999.2-01-SUMMARY.md`
</output>
