---
phase: 07-advanced-orchestration-branching
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [src/server/orchestrator.ts, tests/orchestrator.test.ts, src/server/index.ts]
autonomous: true
requirements: [ORCH-05, ORCH-06, RND-07, RND-08]
---

<objective>
Upgrade the RoundTable orchestrator to support intelligent, adaptive turn selection and consensus detection.
The Blue Hat will now act as a "Conductor," suggesting the next speaker or terminating the loop when consensus is reached.

Purpose: Move from static sequential execution to intelligent agent-driven orchestration.
Output: Updated `runRoundTable` logic with Conductor mode and consensus detection.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-advanced-orchestration-branching/07-CONTEXT.md
@.planning/phases/07-advanced-orchestration-branching/07-RESEARCH.md
@src/server/orchestrator.ts
@tests/orchestrator.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 0: Expand orchestrator tests for Conductor mode</name>
  <files>tests/orchestrator.test.ts</files>
  <action>
    Add new test cases to `tests/orchestrator.test.ts` for the Conductor logic:
    - Test the "Conductor" prompt generation: Ensure it includes the available personas and the latest conversation context.
    - Test the parsing of Blue Hat output: Correctly extracting personaId or `[CONSENSUS_REACHED]`.
    - Test the `max_turns` safety valve: Ensure the loop terminates even if consensus is never reached.
  </action>
  <verify>
    <automated>npx vitest tests/orchestrator.test.ts</automated>
  </verify>
  <done>Orchestrator test suite expanded with Conductor mode requirements.</done>
</task>

<task type="auto">
  <name>Task 1: Implement Blue Hat Conductor mode in orchestrator</name>
  <files>src/server/orchestrator.ts</files>
  <action>
    Modify `runRoundTable` in `src/server/orchestrator.ts` to support a new `mode` (e.g., 'conductor'):
    1.  **Identify Conductor**: The Blue Hat persona should be used as the conductor.
    2.  **Suggestion Logic**: At the start of each round (or after each speaker), if in conductor mode, prompt the Blue Hat to select the next persona from the `availablePersonaIds` list.
    3.  **Conductor Prompt**: Create a prompt that lists available personas (Id, Name, Description) and asks for the next personaId OR `[CONSENSUS_REACHED]`.
    4.  **HITL Support**: If in `hitl` mode, yield a `SUGGESTION` event and then return (ending the current stream).
  </action>
  <verify>
    <automated>npx vitest tests/orchestrator.test.ts</automated>
  </verify>
  <done>Conductor logic implemented in the orchestrator.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Consensus Check and Safety Valve</name>
  <files>src/server/orchestrator.ts, src/server/index.ts</files>
  <action>
    1.  **Consensus Check**: In `runRoundTable`, handle the `[CONSENSUS_REACHED]` token from the Blue Hat. If detected, emit a final summary/conclusion and terminate the loop.
    2.  **Safety Valve**: Implement a `maxTurns` counter (default 10) in `runRoundTable`. Terminate the loop if `currentTurn >= maxTurns`.
    3.  **API Update**: Update the `/sessions/:id/evaluate` endpoint in `src/server/index.ts` to accept `mode` ('sequential', 'auto', 'hitl') and `maxTurns` as query parameters.
  </action>
  <verify>
    <automated>npx vitest tests/orchestrator.test.ts</automated>
  </verify>
  <done>Consensus detection and turn limits are active and configurable via API.</done>
</task>

</tasks>

<verification>
Verify `runRoundTable` can now run in a loop based on Blue Hat suggestions.
Verify the orchestrator stops when `[CONSENSUS_REACHED]` is output by Blue Hat.
Verify `maxTurns` limit stops the execution.
</verification>

<success_criteria>
- [ ] Orchestrator can intelligently pick the next speaker based on conversation context.
- [ ] Consensus reached signal terminates the round-table session.
- [ ] Turn limits prevent infinite loops in automated mode.
</success_criteria>

<must_haves>
  truths:
    - "The Blue Hat suggests the next speaker from the available pool"
    - "The orchestrator respects the consensus reached signal"
    - "The orchestrator terminates after max_turns if no consensus is reached"
  artifacts:
    - path: "src/server/orchestrator.ts"
      provides: "runRoundTable Conductor mode"
  key_links:
    - from: "Blue Hat Output"
      to: "Next Persona Selection"
      via: "parsing and loop logic"
</must_haves>

<output>
After completion, create `.planning/phases/07-advanced-orchestration-branching/07-02-SUMMARY.md`
</output>
