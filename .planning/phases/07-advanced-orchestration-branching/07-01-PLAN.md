---
phase: 07-advanced-orchestration-branching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/server/db/schema.ts, src/server/index.ts, tests/forking.test.ts]
autonomous: true
requirements: [RND-10]
---

<objective>
Establish the database and backend foundation for session branching (forking).
This involves updating the session schema to support lineage and implementing the logic to clone a session's history up to a specific message.

Purpose: Enable users to explore alternative conversation paths from any point in the history.
Output: Updated database schema, forking utility logic, and a new API endpoint.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-advanced-orchestration-branching/07-CONTEXT.md
@.planning/phases/07-advanced-orchestration-branching/07-RESEARCH.md
@src/server/db/schema.ts
@src/server/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create test scaffold for session forking</name>
  <files>tests/forking.test.ts</files>
  <action>
    Create a Vitest test suite that defines the expected behavior for session forking.
    The test should verify:
    - `forkSession` clones the session record with a new ID and `parentId`.
    - `forkSession` clones all messages from the parent session up to and including the specified `messageId`.
    - Cloned messages have new UUIDs but preserve original `role`, `personaId`, `content`, and `timestamp`.
  </action>
  <verify>
    <automated>npx vitest tests/forking.test.ts</automated>
  </verify>
  <done>Test scaffold exists and fails initially (until implementation).</done>
</task>

<task type="auto">
  <name>Task 1: Update Drizzle schema with parentId</name>
  <files>src/server/db/schema.ts</files>
  <action>
    Update the `sessions` table in `src/server/db/schema.ts` to include a `parentId` field.
    The field should be a self-referencing foreign key to `sessions.id`.
    
    ```typescript
    parentId: text('parent_id').references((): any => sessions.id),
    ```
    (Note: Using `(): any => sessions.id` might be necessary to avoid circular reference issues in some Drizzle versions, as suggested in RESEARCH.md).
    
    After updating the schema, run `npx drizzle-kit push` to update the local SQLite database.
  </action>
  <verify>
    <automated>npx drizzle-kit check</automated>
  </verify>
  <done>Schema updated and pushed to database.</done>
</task>

<task type="auto">
  <name>Task 2: Implement session forking logic and API endpoint</name>
  <files>src/server/index.ts</files>
  <action>
    1. Implement a `forkSession` utility function in `src/server/index.ts` (or a separate service file if preferred) that performs the "Total Clone" strategy:
       - Create new session record with `parentId = originalId`.
       - Copy title (e.g., `Fork: ${parent.title}`).
       - Fetch and clone messages from the parent session where `timestamp <= forkPointMessage.timestamp`.
       - Ensure new IDs are generated for all cloned entities.
    2. Add `POST /sessions/:id/fork` endpoint to the Hono app in `src/server/index.ts`.
       - Accepts `messageId` in the request body.
       - Returns the new session ID.
  </action>
  <verify>
    <automated>npx vitest tests/forking.test.ts</automated>
  </verify>
  <done>Forking logic implemented and endpoint is functional.</done>
</task>

</tasks>

<verification>
Check that `parentId` exists in `sessions` table: `sqlite3 sqlite.db ".schema sessions"`
Verify `POST /sessions/:id/fork` returns 201 and creates a new session with cloned messages.
</verification>

<success_criteria>
- [ ] `sessions` table has `parent_id` column.
- [ ] `POST /sessions/:id/fork` correctly clones history up to the target message.
- [ ] New session is created with a reference to the parent session.
</success_criteria>

<must_haves>
  truths:
    - "User can trigger a session fork from a specific message"
    - "The forked session contains the same message history as the parent up to the fork point"
    - "The forked session is independent of the parent session for future messages"
  artifacts:
    - path: "src/server/db/schema.ts"
      provides: "parentId in sessions table"
    - path: "src/server/index.ts"
      provides: "POST /sessions/:id/fork endpoint"
  key_links:
    - from: "POST /sessions/:id/fork"
      to: "db.insert(sessions)"
      via: "cloning logic"
</must_haves>

<output>
After completion, create `.planning/phases/07-advanced-orchestration-branching/07-01-SUMMARY.md`
</output>
