---
phase: 07-advanced-orchestration-branching
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified: [src/components/DebateView.tsx, src/app/sessions/page.tsx, src/components/BranchNavigator.tsx, src/components/RoundTableConfig.tsx]
autonomous: true
requirements: [UI-06, ORCH-07, ORCH-05]
---

<objective>
Complete the Phase 7 implementation by integrating session branching and intelligent orchestration into the user interface.
This involves adding the "Fork" action to messages, building a visual tree for branch navigation, and implementing HITL (Human-in-the-Loop) controls for turn approval.

Purpose: Provide a seamless, visual experience for exploring and controlling conversation branches.
Output: Fork-capable chat interface, visual Branch Tree component, and Turn Suggestion UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-advanced-orchestration-branching/07-CONTEXT.md
@.planning/phases/07-advanced-orchestration-branching/07-RESEARCH.md
@src/components/DebateView.tsx
@src/app/sessions/page.tsx
@src/components/RoundTableConfig.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Fork trigger and Branch Navigator</name>
  <files>src/components/DebateView.tsx, src/app/sessions/page.tsx, src/components/BranchNavigator.tsx</files>
  <action>
    1.  **Fork Button**: In `src/components/DebateView.tsx`, add a "Fork" button (using `GitFork` icon from lucide-react) to the message action bar (next to Edit/Delete).
    2.  **Fork Handler**: Implement `onForkMessage(messageId)` prop in `DebateView`. When clicked, it should call the `POST /sessions/:id/fork` API.
    3.  **Branch Navigator**: Create `src/components/BranchNavigator.tsx`. It should fetch the session tree (sessions where `parentId` or `id` matches the current session family) and display them in a list or simple nested structure.
    4.  **Integration**: Add `BranchNavigator` to the sidebar in `src/app/sessions/page.tsx`.
  </action>
  <verify>
    <automated>ls src/components/BranchNavigator.tsx && grep -n "GitFork" src/components/DebateView.tsx</automated>
  </verify>
  <done>Users can fork sessions from the UI and see related branches in the sidebar.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Visual Branch Tree Navigator</name>
  <files>src/components/BranchNavigator.tsx</files>
  <action>
    Upgrade `src/components/BranchNavigator.tsx` to use a visual tree representation:
    - Use a custom SVG-based component or a library like `react-flow` (if installed) to render the session lineage.
    - Each node in the tree represents a session. The current session should be highlighted.
    - Clicking a node should navigate to that session (updating the `id` query param).
    - Ensure the tree layout is clear and responsive.
  </action>
  <verify>
    <automated>grep -n "svg" src/components/BranchNavigator.tsx || grep -n "react-flow" src/components/BranchNavigator.tsx</automated>
  </verify>
  <done>Visual tree allows clear navigation between multiple conversation paths.</done>
</task>

<task type="auto">
  <name>Task 3: Add HITL Turn Approval Controls</name>
  <files>src/components/RoundTableConfig.tsx, src/app/sessions/page.tsx</files>
  <action>
    1.  **Mode Toggle**: In `src/components/RoundTableConfig.tsx`, add a toggle for "Orchestration Mode" (Sequential, Conductor Auto, Conductor HITL).
    2.  **Suggestion Handling**: In `src/app/sessions/page.tsx`, handle the `SUGGESTION` event from the SSE stream. When received, pause the `isLoading` state and show a "Suggested Next Turn" UI.
    3.  **Approval UI**: Create a component (or update `RoundTableConfig`) to show the suggested persona and provide "Approve" and "Change" buttons.
    4.  **Resume Execution**: When "Approve" is clicked, trigger a new evaluation with the suggested persona and `autoResume: true`.
  </action>
  <verify>
    <automated>grep -n "mode" src/components/RoundTableConfig.tsx</automated>
  </verify>
  <done>HITL turn approval controls integrated and functional.</done>
</task>

</tasks>

<verification>
Manual visual check of the Tree navigator.
Verify forking a message correctly redirects to the new session with history.
Verify HITL mode pauses and waits for user approval before continuing the round-table.
</verification>

<success_criteria>
- [ ] Visual tree allows clear navigation between multiple conversation paths.
- [ ] User can fork a session from any message and continue independently.
- [ ] HITL controls correctly pause and resume the orchestration loop based on user input.
</success_criteria>

<must_haves>
  truths:
    - "The UI displays a 'Fork' button on messages"
    - "A visual tree navigator shows session lineage"
    - "User can approve or override the Blue Hat's turn suggestion"
  artifacts:
    - path: "src/components/BranchNavigator.tsx"
      provides: "Visual tree navigation"
    - path: "src/components/DebateView.tsx"
      provides: "Fork button integration"
  key_links:
    - from: "Fork Button"
      to: "/api/sessions/:id/fork"
      via: "fetch call"
</must_haves>

<output>
After completion, create `.planning/phases/07-advanced-orchestration-branching/07-03-SUMMARY.md`
</output>
