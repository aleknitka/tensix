---
phase: 12-onboarding-agent
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [src/server/orchestrator.ts, src/server/index.ts, src/server/services/refinement-service.ts]
autonomous: true
requirements: [REQ-12.4, REQ-12.6]

must_haves:
  truths:
    - "Refinement dialogue streams correctly using the Refiner persona"
    - "Confirmation updates session status to 'active'"
    - "Round-table uses refined prompt if available"
  artifacts:
    - path: "src/server/services/refinement-service.ts"
      provides: "Implemented runRefinement generator"
    - path: "src/server/orchestrator.ts"
      provides: "Updated runRoundTable for refined context"
    - path: "src/server/index.ts"
      provides: "New endpoints for refinement control"
  key_links:
    - from: "src/server/index.ts"
      to: "src/server/services/refinement-service.ts"
      via: "API call"
---

<objective>
Implement the core orchestration logic for the Socratic dialogue and provide the necessary API endpoints.

Purpose: Enable streaming dialogue with the Refiner and allow users to commit their refined problem statements.
Output: Streaming refinement API, state transition endpoints, and updated round-table logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/server/orchestrator.ts
@src/server/index.ts
@src/server/services/refinement-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Refinement Generator</name>
  <files>src/server/services/refinement-service.ts</files>
  <action>
    Implement the `runRefinement` async generator.
    - Fetch the 'Refiner' persona.
    - Get the current session messages and documents.
    - Use the Refiner persona to generate a streaming response clarifying the user's initial prompt.
    - Ensure it uses the existing `BaseProvider.generate` interface.
  </action>
  <verify>
    <automated>npx vitest tests/refinement.test.ts</automated>
  </verify>
  <done>Refinement generator implemented and streaming correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Transition API Endpoints</name>
  <files>src/server/index.ts</files>
  <action>
    Expose the following endpoints in the Hono server:
    - `GET /sessions/:id/refine` - Streaming endpoint using `runRefinement`.
    - `POST /sessions/:id/refine/confirm` - Updates session to `active` and saves `refinedPrompt`.
    - `POST /sessions/:id/refine/skip` - Updates session to `active` immediately.
    - `GET /sessions/:id` - Ensure this returns the session status and refined prompt.
  </action>
  <verify>
    <automated>curl -I http://localhost:3001/sessions/any-id/refine</automated>
  </verify>
  <done>Transition APIs exposed and functional.</done>
</task>

<task type="auto">
  <name>Task 3: Refined Prompt Injection</name>
  <files>src/server/orchestrator.ts</files>
  <action>
    Modify `runRoundTable` to prioritize the `refinedPrompt`.
    - If `session.refinedPrompt` exists, prepend it as a system message: "[REFINED CONTEXT]: " + `refinedPrompt`.
    - Ensure expert personas respond to this refined context rather than just the initial user message.
  </action>
  <verify>
    <automated>npx vitest tests/orchestrator.test.ts</automated>
  </verify>
  <done>Round-table orchestration successfully incorporates refined intent.</done>
</task>

</tasks>

<verification>
Manual test with curl/postman to ensure endpoints update the DB and stream correctly. Run existing tests to ensure no regressions.
</verification>

<success_criteria>
- `/sessions/:id/refine` streams content.
- `/confirm` and `/skip` successfully update the session record.
- `runRoundTable` includes the refined context in its generation loop.
</success_criteria>

<output>
After completion, create `.planning/phases/12-onboarding-agent/12-02-SUMMARY.md`
</output>
