---
phase: 04-persona-management-moderation
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [src/server/index.ts, src/components/DebateView.tsx]
autonomous: true
requirements: [UI-02]

must_haves:
  truths:
    - "User can edit the content of any message (their own or a persona's)"
    - "User can delete any message from the session history"
    - "Edited content is immediately reflected in the UI and persistent across reloads"
    - "User can manually insert a turn as a specific persona (without LLM generation)"
  artifacts:
    - path: "src/server/index.ts"
      provides: "Message edit/delete/insert API endpoints"
      exports: ["PATCH /sessions/:id/messages/:messageId", "DELETE /sessions/:id/messages/:messageId", "POST /sessions/:id/messages"]
    - path: "src/components/DebateView.tsx"
      provides: "Inline editing, deletion, and manual turn insertion UI"
  key_links:
    - from: "src/components/DebateView.tsx"
      to: "/sessions/:id/messages"
      via: "fetch (POST/PATCH/DELETE)"
      pattern: "fetch.*messages"
---

<objective>
Enable message-level moderation (UI-02). Allow users to manually intervene by editing, deleting, or inserting messages within a round-table session.

Purpose: Correct errors, prune irrelevant turns, or override undesirable outputs from personas.
Output: API endpoints for message management and UI controls in the Debate view.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/server/db/schema.ts
@src/server/index.ts
@src/components/DebateView.tsx

<interfaces>
From src/server/db/schema.ts:
```typescript
export const messages = sqliteTable('messages', {
  id: text('id').primaryKey(),
  sessionId: text('session_id').references(() => sessions.id, { onDelete: 'cascade' }),
  personaId: text('persona_id').references(() => personas.id),
  role: text('role').notNull(),
  content: text('content').notNull(),
  timestamp: integer('timestamp', { mode: 'timestamp' }).notNull(),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Message Moderation API</name>
  <files>src/server/index.ts</files>
  <action>
    Modify `src/server/index.ts`:
    - Ensure `POST /sessions/:id/messages` supports optional `personaId` and `role`.
    - Add `PATCH /sessions/:id/messages/:messageId`: Update message content by ID.
    - Add `DELETE /sessions/:id/messages/:messageId`: Remove message by ID.
  </action>
  <verify>
    <automated>
      # Assuming a message with ID "TEST_MSG" exists
      # curl -X PATCH http://localhost:3001/sessions/sid/messages/TEST_MSG -d '{"content":"Edited!"}'
    </automated>
  </verify>
  <done>Backend supports editing, deleting, and manually creating messages with specific personas.</done>
</task>

<task type="auto">
  <name>Task 2: Add Editing UI to DebateView</name>
  <files>src/components/DebateView.tsx</files>
  <action>
    Modify `src/components/DebateView.tsx`:
    - Add "Pencil" (Edit) and "Trash" (Delete) icons next to each message (visible on hover).
    - Implement inline editing: When "Edit" is clicked, replace message text with a Textarea and "Save/Cancel" buttons.
    - Connect buttons to the new API routes.
    - Add a confirmation prompt before deleting.
  </action>
  <verify>
    <automated>MISSING — Manual visual check needed (checkpoint:human-verify)</automated>
  </verify>
  <done>User can edit and delete messages directly in the UI.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Manual Turn Insertion</name>
  <files>src/components/DebateView.tsx</files>
  <action>
    Add a "Plus" button or a dropdown next to the main message input that allows the user to select a Persona.
    If a Persona is selected, the message sent will be attributed to that Persona (role: 'assistant', personaId: selected_id) instead of the Proponent (role: 'user').
  </action>
  <verify>
    <automated>MISSING — Manual visual check needed (checkpoint:human-verify)</automated>
  </verify>
  <done>User can insert a manual turn as any available persona.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Message Editing/Deletion/Insertion UI</what-built>
  <how-to-verify>
    1. Open an existing session with messages.
    2. Hover over a persona response and click "Edit".
    3. Change the text and click "Save". Verify the change persists.
    4. Delete a message and verify it's gone.
    5. Use the new Persona selector in the input area to send a message *as* the "White Hat".
    6. Verify the message appears with the White Hat avatar and name.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Check DB records directly if possible or use GET /messages to verify persistent state.
</verification>

<success_criteria>
1. User can successfully edit any message in a session.
2. User can successfully delete any message in a session.
3. User can insert a message as any persona.
</success_criteria>

<output>
After completion, create `.planning/phases/04-persona-management-moderation/04-02-SUMMARY.md`
</output>
