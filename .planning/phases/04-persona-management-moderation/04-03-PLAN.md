---
phase: 04-persona-management-moderation
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: [src/components/RoundTableConfig.tsx, src/app/sessions/page.tsx, src/server/orchestrator.ts]
autonomous: true
requirements: [ORCH-02, UI-02]

must_haves:
  truths:
    - "User can toggle between 'Auto' and 'Manual' orchestration modes"
    - "In Manual mode, the round-table stops after each persona turn"
    - "User can manually trigger the next persona turn by selecting a hat and clicking 'Trigger'"
  artifacts:
    - path: "src/components/RoundTableConfig.tsx"
      provides: "Manual mode toggle and single-turn triggers"
    - path: "src/app/sessions/page.tsx"
      provides: "Session-level orchestration state management"
  key_links:
    - from: "src/components/RoundTableConfig.tsx"
      to: "/sessions/:id/evaluate"
      via: "EventSource"
---

<objective>
Implement orchestration-level moderation and HITL controls (ORCH-02, UI-02). Enable users to pause the sequential flow and manually decide when and which persona speaks next.

Purpose: Maintain total control over the direction of the round-table.
Output: Orchestration mode toggle and granular turn controls.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/server/orchestrator.ts
@src/components/RoundTableConfig.tsx
@src/app/sessions/page.tsx

<interfaces>
From src/server/orchestrator.ts:
```typescript
export async function* runRoundTable(sessionId: string, personaIds: string[]): AsyncIterable<OrchestrationStep> {
  // ... implementation ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Manual Mode Toggle</name>
  <files>src/components/RoundTableConfig.tsx</files>
  <action>
    Modify `src/components/RoundTableConfig.tsx`:
    - Add a Switch or Toggle for "Manual Mode" (labeled "HITL Control" or "Step-by-Step").
    - When Manual Mode is ON, change the behavior of the "Start Audit" button to only process the first selected persona in the list.
    - Provide a way to trigger a specific single persona turn (e.g., a "Play" icon next to each persona in the list when Manual mode is on).
  </action>
  <verify>
    <automated>Check if toggle state is correctly handled in the component state.</automated>
  </verify>
  <done>UI supports toggling between automatic and manual orchestration.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Single-Turn Orchestration Logic</name>
  <files>src/app/sessions/page.tsx</files>
  <action>
    Modify `handleStartEvaluation` in `src/app/sessions/page.tsx`:
    - If Manual Mode is on, only pass the *next* persona ID to the `/evaluate` endpoint.
    - After the turn finishes, display a "Next Move" prompt in the sidebar.
    - If the user selects multiple hats in manual mode, they should be processed one-by-one with user confirmation between them.
  </action>
  <verify>
    <automated>MISSING — Manual test needed</automated>
  </verify>
  <done>Session page handles step-by-step turn execution.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Manual Orchestration & Single-Turn Triggers</what-built>
  <how-to-verify>
    1. Start a session and select multiple personas.
    2. Toggle "Manual Mode" to ON.
    3. Click "Start Audit". Verify only the first persona generates a response, then the system waits.
    4. Verify that you can edit the persona's response (from Plan 4-02).
    5. Trigger the next persona turn manually.
    6. Toggle "Manual Mode" OFF and verify it resumes automatic sequential execution for the remaining hats.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Check SSE logs to ensure that in manual mode, the server is only called for a single personaId at a time or the loop is controlled by the client.
</verification>

<success_criteria>
1. User can manually control the turn sequence.
2. Debate pauses after each turn in Manual mode.
3. User can intervene (edit) between manual turns.
</success_criteria>

<output>
After completion, create `.planning/phases/04-persona-management-moderation/04-03-SUMMARY.md`
</output>
