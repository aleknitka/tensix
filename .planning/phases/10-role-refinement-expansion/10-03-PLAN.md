---
phase: 10-role-refinement-expansion
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified: [src/components/RoleSelector.tsx, src/app/settings/page.tsx, src/components/RoundTableConfig.tsx, src/components/PersonaEditor.tsx, src/server/index.ts]
autonomous: true
requirements: [UI-01, UI-02, ROLE-03]

must_haves:
  truths:
    - "Users can search and select roles from the predefined library in the Round Table view"
    - "The Settings page has a dedicated Role Library section to browse predefined roles"
    - "Adding a role from the library allows assigning a model to it"
    - "Users can download a role profile as a .yml file from the Role Library UI"
    - "Users can upload a .yml file to import it into their persona library"
  artifacts:
    - path: "src/components/RoleSelector.tsx"
      provides: "Searchable dropdown for role selection"
    - path: "src/app/settings/page.tsx"
      provides: "Updated settings with Role Library management and import"
    - path: "src/components/RoundTableConfig.tsx"
      provides: "Integrated role library selection"
    - path: "src/components/PersonaEditor.tsx"
      provides: "Export functionality for individual roles"
  key_links:
    - from: "src/components/PersonaEditor.tsx"
      to: "browser download"
      via: "js-yaml and Blob"
    - from: "src/app/settings/page.tsx"
      to: "POST /personas"
      via: "YAML file upload and parsing"
---

<objective>
Implement the user interface for browsing the role library, selecting roles for sessions, and managing roles in the settings, including YAML-based portability (export/import).

Purpose: Provide a user-friendly way to leverage the portable role library and customize persona behavior through manual export/import.
Output: RoleSelector component, updated Settings page, integrated Round Table configuration, and YAML portability tools.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-role-refinement-expansion/10-02-SUMMARY.md
@.planning/phases/10-role-refinement-expansion/10-CONTEXT.md
@.planning/phases/10-role-refinement-expansion/10-RESEARCH.md
@src/components/RoundTableConfig.tsx
@src/components/PersonaEditor.tsx
@src/app/settings/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RoleSelector component</name>
  <files>src/components/RoleSelector.tsx</files>
  <action>
    Create a `RoleSelector.tsx` component that:
    1. Fetches predefined roles (`is_predefined=true`) from the `/personas` endpoint.
    2. Provides a searchable input field.
    3. Displays a list of roles matching the search query.
    4. Shows role details (name, description, role_type) in the list.
    5. Allows selecting a role and triggers an `onSelect(role)` callback.
    
    Use `lucide-react` for the search icon.
  </action>
  <verify>
    <automated>ls src/components/RoleSelector.tsx</automated>
  </verify>
  <done>RoleSelector component created with search and selection logic</done>
</task>

<task type="auto">
  <name>Task 2: Update Settings with Role Library section</name>
  <files>src/app/settings/page.tsx, src/components/PersonaEditor.tsx</files>
  <action>
    1. Update `src/app/settings/page.tsx` to include a "Role Library" section.
    2. Update `src/components/PersonaEditor.tsx` to:
       - Distinguish between "Custom Personas" and "Library Roles" (predefined).
       - Allow editing `chattiness_limit` and `role_type` for personas.
       - Prevent deletion of predefined roles from the UI (only custom ones).
       - Allow "Cloning" a predefined role into a custom persona.
  </action>
  <verify>
    <automated>grep "Role Library" src/app/settings/page.tsx</automated>
  </verify>
  <done>Settings page and PersonaEditor updated to handle the role library and new behavioral fields</done>
</task>

<task type="auto">
  <name>Task 3: Integrate RoleSelector into RoundTableConfig</name>
  <files>src/components/RoundTableConfig.tsx</files>
  <action>
    Update `src/components/RoundTableConfig.tsx` to:
    1. Replace or augment the "Seed Hats" logic with a "Add from Library" button that opens the `RoleSelector`.
    2. When a role is selected from the library:
       - Check if a persona with that `id` already exists in the session/database with a model assigned.
       - If it doesn't have a model, prompt the user to select a model.
       - Add the selected role to the active participants list.
    3. Ensure the UI reflects roles added from the library correctly.
  </action>
  <verify>
    <automated>grep "RoleSelector" src/components/RoundTableConfig.tsx</automated>
  </verify>
  <done>Round Table configuration view allows adding specialized roles from the library</done>
</task>

<task type="auto">
  <name>Task 4: Implement Role Export & Import (YAML)</name>
  <files>src/components/PersonaEditor.tsx, src/app/settings/page.tsx</files>
  <action>
    Implement manual YAML portability for roles:
    1. **Export**: In `PersonaEditor.tsx`, add an "Export to YAML" button. Use `js-yaml` to dump the persona fields into a YAML string and trigger a browser download for `{persona.id}.yml`.
    2. **Import**: In `src/app/settings/page.tsx`, add an "Import Role (YAML)" button or file drop area in the Role Library section. 
    3. **Validation**: When a file is uploaded, parse it with `js-yaml`, validate against the `RoleSchema` (from 10-02), and save it to the database as a new custom persona via `POST /personas`.
    
    Fields to export/import: `id`, `name`, `description`, `systemPrompt`, `chattiness_limit`, `role_type`.
  </action>
  <verify>
    <automated>grep "Export to YAML" src/components/PersonaEditor.tsx && grep "Import" src/app/settings/page.tsx</automated>
  </verify>
  <done>Users can export existing roles to YAML and import new roles from YAML files via the UI</done>
</task>

</tasks>

<verification>
1. Open Settings > Role Library. 
2. Select a role and click "Export to YAML". Verify a .yml file is downloaded.
3. Open the downloaded file and change its 'name' and 'id' fields.
4. Use the "Import Role" button in Settings to upload the modified file.
5. Verify the new role appears in the library.
</verification>

<success_criteria>
- [ ] Users can browse and search the role library in the UI.
- [ ] Users can add library roles to a session.
- [ ] Predefined roles are clearly distinguished from custom ones in Settings.
- [ ] YAML export/import of a role profile works correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/10-role-refinement-expansion/10-03-SUMMARY.md`
</output>
