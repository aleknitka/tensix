---
phase: 10-role-refinement-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/server/db/schema.ts, roles/white-hat.yml, roles/red-hat.yml, roles/black-hat.yml, roles/yellow-hat.yml, roles/green-hat.yml, roles/blue-hat.yml]
autonomous: true
requirements: [ROLE-01, DB-01]

must_haves:
  truths:
    - "New columns exist in the personas table (chattiness_limit, role_type, is_predefined)"
    - "Six Hats YAML files exist in the roles/ directory"
    - "Required dependencies are installed"
  artifacts:
    - path: "src/server/db/schema.ts"
      provides: "Updated database schema for personas"
    - path: "roles/white-hat.yml"
      provides: "White Hat role definition"
    - path: "package.json"
      provides: "New dependencies (js-yaml, zod, fast-glob)"
  key_links:
    - from: "roles/*.yml"
      to: "personas table"
      via: "role-sync service (planned for Wave 2)"
---

<objective>
Setup the foundation for the YAML-based role library by installing necessary dependencies, updating the database schema, and creating initial role definitions.

Purpose: Enable portability and specialized constraints for persona roles.
Output: Updated schema, installed libraries, and initial role library files.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-role-refinement-expansion/10-CONTEXT.md
@.planning/phases/10-role-refinement-expansion/10-RESEARCH.md
@src/server/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies</name>
  <files>package.json</files>
  <action>
    Install the following libraries and their types:
    - `js-yaml` (YAML parsing)
    - `zod` (Schema validation)
    - `fast-glob` (File discovery)
    
    Command: `npm install js-yaml zod fast-glob && npm install --save-dev @types/js-yaml`
  </action>
  <verify>
    <automated>npm list js-yaml zod fast-glob</automated>
  </verify>
  <done>Dependencies listed in package.json and installed in node_modules</done>
</task>

<task type="auto">
  <name>Task 2: Update database schema</name>
  <files>src/server/db/schema.ts</files>
  <action>
    Modify the `personas` table in `src/server/db/schema.ts` to include:
    - `chattiness_limit`: integer
    - `role_type`: text (e.g., 'auditor', 'researcher', 'summarizer', 'critic', 'other')
    - `is_predefined`: integer as boolean (default false)
    - `description`: text (optional)

    Also, make `modelId` nullable by removing `.notNull()` to support role templates that don't have an assigned model yet.
  </action>
  <verify>
    <automated>grep -E "chattiness_limit|role_type|is_predefined" src/server/db/schema.ts</automated>
  </verify>
  <done>Schema file contains the new columns and modelId is now nullable</done>
</task>

<task type="auto">
  <name>Task 3: Create initial Role Library (Six Hats)</name>
  <files>roles/white-hat.yml, roles/red-hat.yml, roles/black-hat.yml, roles/yellow-hat.yml, roles/green-hat.yml, roles/blue-hat.yml</files>
  <action>
    Create a `roles/` directory at the project root.
    Populate it with YAML files for the Six Thinking Hats based on `src/server/db/seeds/personas.ts`.
    
    Each file should follow this structure:
    ```yaml
    id: "white-hat"
    name: "White Hat"
    role_type: "auditor"
    description: "Focuses on facts, data, and information available."
    systemPrompt: "You are the White Hat. Focus on the facts..."
    chattiness_limit: 4
    ```

    Role types:
    - White, Red, Black, Yellow, Green: auditor
    - Blue: summarizer
  </action>
  <verify>
    <automated>ls -l roles/*.yml && cat roles/white-hat.yml</automated>
  </verify>
  <done>Six YAML files exist in roles/ directory with correct fields</done>
</task>

</tasks>

<verification>
Check that the schema compiles and the roles directory is populated.
</verification>

<success_criteria>
- [ ] Dependencies installed.
- [ ] `personas` table schema updated.
- [ ] `roles/` directory contains 6 predefined hats in YAML format.
</success_criteria>

<output>
After completion, create `.planning/phases/10-role-refinement-expansion/10-01-SUMMARY.md`
</output>
