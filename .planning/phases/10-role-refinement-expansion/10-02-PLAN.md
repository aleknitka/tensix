---
phase: 10-role-refinement-expansion
plan: 02
type: execute
wave: 2
depends_on: [10-role-refinement-expansion-01]
files_modified: [src/server/services/role-schema.ts, src/server/services/role-sync.ts, src/server/orchestrator.ts, src/server/index.ts, tests/roles.test.ts, tests/orchestrator.test.ts]
autonomous: true
requirements: [ROLE-02, CHAT-01]

must_haves:
  truths:
    - "Role sync runs on startup and upserts roles from YAML files into the database"
    - "Orchestrator applies anti-chattiness suffix to system prompts based on role type/overrides"
    - "Predefined roles have is_predefined = true in the database"
  artifacts:
    - path: "src/server/services/role-schema.ts"
      provides: "Zod schema for role validation"
    - path: "src/server/services/role-sync.ts"
      provides: "Sync service for YAML roles"
    - path: "src/server/orchestrator.ts"
      provides: "Updated runRoundTable with conciseness enforcement"
    - path: "tests/roles.test.ts"
      provides: "Test suite for role management and sync"
    - path: "tests/orchestrator.test.ts"
      provides: "Test suite for orchestrator behavior"
---

<objective>
Implement the core logic for syncing YAML roles into the database and enforcing anti-chattiness constraints in the orchestrator.

Purpose: Automate role management and improve response quality through structured constraints.
Output: Role validation schema, sync service, updated orchestrator, and automated test suites.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-role-refinement-expansion/10-01-SUMMARY.md
@.planning/phases/10-role-refinement-expansion/10-CONTEXT.md
@.planning/phases/10-role-refinement-expansion/10-RESEARCH.md
@src/server/orchestrator.ts
@src/server/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create test scaffolds</name>
  <files>tests/roles.test.ts, tests/orchestrator.test.ts</files>
  <action>
    Create test files for the new functionality:
    1. `tests/roles.test.ts`: Should test YAML parsing, Zod validation, and database upserts.
    2. `tests/orchestrator.test.ts`: Should test `applyChattinessConstraint` with various role types and overrides.
    
    Use `vitest` for testing as established in the project.
  </action>
  <verify>
    <automated>ls tests/roles.test.ts tests/orchestrator.test.ts</automated>
  </verify>
  <done>Test files created and ready for implementation</done>
</task>

<task type="auto">
  <name>Task 1: Define Role Schema validation</name>
  <files>src/server/services/role-schema.ts</files>
  <action>
    Create a Zod schema for role validation as recommended in the research.
    
    Fields:
    - `id`: string
    - `name`: string
    - `description`: string (optional)
    - `role_type`: enum(['auditor', 'researcher', 'summarizer', 'critic', 'other'])
    - `systemPrompt`: string
    - `chattiness_limit`: number (optional)

    Export the schema and the TypeScript type.
  </action>
  <verify>
    <automated>npm test tests/roles.test.ts</automated>
  </verify>
  <done>Zod schema exported from the file and passes basic validation tests</done>
</task>

<task type="auto">
  <name>Task 2: Implement Role Sync service</name>
  <files>src/server/services/role-sync.ts, src/server/index.ts</files>
  <action>
    Implement a sync service in `src/server/services/role-sync.ts` that:
    1. Uses `fast-glob` to find all `*.yml` files in the `roles/` directory.
    2. Parses them with `js-yaml`.
    3. Validates them with the Zod schema.
    4. Upserts the roles into the `personas` table.
    
    Mapping:
    - `id` -> `id`
    - `name` -> `name`
    - `description` -> `role` (for UI display compatibility) and `description`
    - `role_type` -> `role_type`
    - `systemPrompt` -> `systemPrompt`
    - `chattiness_limit` -> `chattiness_limit`
    - `is_predefined` -> true
    
    Initialize this service in `src/server/index.ts` by calling a `syncRoles()` function on startup.
  </action>
  <verify>
    <automated>npm test tests/roles.test.ts</automated>
  </verify>
  <done>Sync logic implemented, passes tests, and called on server startup</done>
</task>

<task type="auto">
  <name>Task 3: Update Orchestrator for anti-chattiness</name>
  <files>src/server/orchestrator.ts</files>
  <action>
    Update `src/server/orchestrator.ts` to enforce conciseness.
    
    Implement a `applyChattinessConstraint(persona, prompt)` function:
    - Default suffix: "Keep your response extremely concise (max 3-4 sentences) unless your role is 'researcher' or 'summarizer'."
    - If `persona.chattiness_limit` is set, use it: "Constraint: Your response must be under ${persona.chattiness_limit} sentences."
    - Exempt `role_type` 'researcher' and 'summarizer' from the default suffix.
    
    Apply this function to the system prompt in the `runRoundTable` loop before sending it to the adapter.
  </action>
  <verify>
    <automated>npm test tests/orchestrator.test.ts</automated>
  </verify>
  <done>Orchestrator modifies system prompts to enforce conciseness based on role settings, verified by tests</done>
</task>

</tasks>

<verification>
Start the server and check the database to see if predefined roles were synced. Run a round-table session and verify that standard roles produce concise responses.
</verification>

<success_criteria>
- [ ] Roles are synced from YAML to DB on startup.
- [ ] Zod validation prevents malformed roles from being synced.
- [ ] LLM responses for standard roles follow the sentence limit.
- [ ] All new logic is covered by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/10-role-refinement-expansion/10-02-SUMMARY.md`
</output>
