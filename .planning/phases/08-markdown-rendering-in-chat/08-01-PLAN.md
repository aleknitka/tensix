---
phase: 08-markdown-rendering-in-chat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/app/layout.tsx, src/app/globals.css, src/components/ThemeProvider.tsx, vitest.config.ts]
autonomous: true
requirements: [REND-02]

must_haves:
  truths:
    - "Dark mode is managed by next-themes"
    - "Tailwind v4 typography plugin is active"
    - "Vitest is configured for component testing"
  artifacts:
    - path: "src/components/ThemeProvider.tsx"
      provides: "next-themes provider wrapper"
    - path: "vitest.config.ts"
      provides: "Vitest configuration"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/ThemeProvider.tsx"
      via: "wrapping children"
---

<objective>
Setup dependencies and foundational configurations for Markdown rendering and robust dark mode support.

Purpose: Ensure the project has all necessary libraries and theme management infrastructure before implementing rich text rendering.
Output: Updated package.json, ThemeProvider component, and Tailwind/Vitest configurations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-markdown-rendering-in-chat/08-CONTEXT.md
@.planning/phases/08-markdown-rendering-in-chat/08-RESEARCH.md
@src/app/layout.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and setup test script</name>
  <files>package.json</files>
  <action>
    - Install core Markdown and styling libraries:
      - `react-markdown` (v10)
      - `remark-gfm`
      - `react-syntax-highlighter`
      - `next-themes`
      - `@tailwindcss/typography` (as dev dependency)
      - `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom` (for testing)
    - Add `"test": "vitest run"` to the `scripts` section of `package.json`.
  </action>
  <verify>
    <automated>npm list react-markdown remark-gfm react-syntax-highlighter next-themes @tailwindcss/typography vitest && grep -q "\"test\": \"vitest run\"" package.json</automated>
  </verify>
  <done>All required dependencies are present and the test script is configured in package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Setup next-themes and ThemeProvider</name>
  <files>src/components/ThemeProvider.tsx, src/app/layout.tsx</files>
  <action>
    - Create `src/components/ThemeProvider.tsx` as a Client Component exporting a `ThemeProvider` wrapper using `next-themes`.
    - Update `src/app/layout.tsx` to:
      - Add `suppressHydrationWarning` to the `html` tag.
      - Wrap the body content with the new `ThemeProvider` (attribute="class", defaultTheme="system", enableSystem).
  </action>
  <verify>
    <automated>grep -q "ThemeProvider" src/app/layout.tsx && grep -q "suppressHydrationWarning" src/app/layout.tsx</automated>
  </verify>
  <done>ThemeProvider is integrated into the root layout and hydration warnings are suppressed.</done>
</task>

<task type="auto">
  <name>Task 3: Configure Tailwind v4 Typography and Vitest</name>
  <files>src/app/globals.css, vitest.config.ts</files>
  <action>
    - Update `src/app/globals.css`:
      - Add `@plugin "@tailwindcss/typography";` for Tailwind v4.
      - Ensure dark mode variant is correctly configured: `@variant dark (&:where(.dark, .dark *));`.
    - Create `vitest.config.ts` with React/jsdom support and alias configuration if needed (matching tsconfig).
  </action>
  <verify>
    <automated>grep -q "@plugin \"@tailwindcss/typography\"" src/app/globals.css && ls vitest.config.ts</automated>
  </verify>
  <done>Tailwind typography plugin is loaded and Vitest is ready for testing.</done>
</task>

</tasks>

<verification>
Check if the application still builds and runs correctly with the new layout wrapper.
</verification>

<success_criteria>
- All dependencies are installed.
- ThemeProvider is active and dark mode can be toggled (manually or via system).
- Tailwind typography classes are available.
- Vitest can execute a simple test.
</success_criteria>

<output>
After completion, create `.planning/phases/08-markdown-rendering-in-chat/08-01-SUMMARY.md`
</output>
