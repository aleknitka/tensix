---
phase: 08-markdown-rendering-in-chat
plan: 03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified: [src/components/DebateView.tsx]
autonomous: false
requirements: [REND-01, REND-02, REND-03]

must_haves:
  truths:
    - "DebateView uses the Markdown component for both static and streaming messages"
    - "Dark mode is consistent across the chat UI"
    - "Rich text formatting is visible in the chat"
  artifacts:
    - path: "src/components/DebateView.tsx"
      provides: "Updated chat UI with Markdown support"
  key_links:
    - from: "src/components/DebateView.tsx"
      to: "src/components/Markdown.tsx"
      via: "component import"
---

<objective>
Integrate the Markdown component into the DebateView and verify its functionality across streaming and static states.

Purpose: Finalize the UI upgrade by replacing plain-text message rendering with the new rich Markdown engine.
Output: Fully functional chat UI with rich text support and dark mode consistency.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-markdown-rendering-in-chat/08-CONTEXT.md
@.planning/phases/08-markdown-rendering-in-chat/08-RESEARCH.md
@src/components/DebateView.tsx
@src/components/Markdown.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DebateView to use the Markdown component</name>
  <files>src/components/DebateView.tsx</files>
  <action>
    - Import the `Markdown` component in `src/components/DebateView.tsx`.
    - Replace the message content `div` with the `Markdown` component inside the messages mapping.
    - Pass the appropriate `variant` prop to the `Markdown` component (e.g., `variant="user"` if `m.role === 'user'`).
    - Remove the `whitespace-pre-wrap` class as the Markdown component handles formatting.
    - Update the `streamingMessage` rendering block to also use the `Markdown` component.
  </action>
  <verify>
    <automated>grep -q "Markdown" src/components/DebateView.tsx</automated>
  </verify>
  <done>DebateView is updated to use the Markdown component for all messages.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Rich text chat interface with Markdown support</what-built>
  <how-to-verify>
    1. Start a session and send a message.
    2. Check if bold, italics, and headers render correctly.
    3. Ask an LLM to generate a table and verify it renders cleanly.
    4. Ask an LLM to generate a code block and verify:
       - Syntax highlighting is visible.
       - "Copy" button appears on hover.
       - "Copy" button works and provides feedback.
    5. Toggle system dark mode and verify consistent styling.
    6. Verify that streaming messages still show the "Speaking..." animation or indicator without breaking Markdown layout.
  </how-to-verify>
  <resume-signal>Type "approved" once verified.</resume-signal>
</task>

</tasks>

<verification>
Perform visual inspection across light and dark modes. Ensure streaming messages are still readable during the process.
</verification>

<success_criteria>
- All messages (static and streaming) render via Markdown.
- Markdown elements are styled correctly.
- Code blocks are highlighted and copyable.
- Dark mode is supported correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/08-markdown-rendering-in-chat/08-03-SUMMARY.md`
</output>
