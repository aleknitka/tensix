---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - server/db/schema.ts
  - server/db/index.ts
  - server/package.json
  - server/index.ts
autonomous: true
requirements: [ORCH-04]
must_haves:
  truths:
    - "Sessions can be created and retrieved via the API"
    - "Database schema includes tables for sessions and messages"
  artifacts:
    - path: "server/db/schema.ts"
      provides: "Drizzle table definitions"
    - path: "server/data.sqlite"
      provides: "SQLite persistence file"
  key_links:
    - from: "server/index.ts"
      to: "server/db/index.ts"
      via: "Drizzle queries"
---

<objective>
Implement the persistence layer using Drizzle ORM and SQLite (via Bun).
Purpose: Enable session storage and retrieval as per the project requirements.
Output: A configured database with schemas for sessions and messages, and API endpoints to interact with them.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Drizzle with Bun's SQLite</name>
  <files>server/package.json, server/db/index.ts</files>
  <action>
    - Install Drizzle ORM and SQLite: `cd server && bun add drizzle-orm @libsql/client`. (Actually, Bun has its own SQLite: `bun add drizzle-orm`, and `bun add -D drizzle-kit`).
    - Create `server/db/index.ts` to initialize the database using Bun's native SQLite driver.
    - Set up a `Database` instance pointing to `data.sqlite`.
  </action>
  <verify>
    <automated>cd server && [ -f db/index.ts ] && grep -q "drizzle" db/index.ts</automated>
  </verify>
  <done>Drizzle and SQLite configured in the backend.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define Sessions and Messages Schema</name>
  <files>server/db/schema.ts</files>
  <behavior>
    - A `sessions` table must exist with: id (UUID), title (text), createdAt (timestamp).
    - A `messages` table must exist with: id (UUID), sessionId (UUID, foreign key), role (text: user/assistant/system), content (text), createdAt (timestamp).
  </behavior>
  <action>
    - Define the schemas in `server/db/schema.ts`.
    - Run initial migration or `push` to create the `data.sqlite` file.
  </action>
  <verify>
    <automated>cd server && bunx drizzle-kit generate:sqlite --schema=db/schema.ts && [ -f drizzle/0000_*.sql ]</automated>
  </verify>
  <done>Database schema defined and initial migration generated.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Session API Endpoints</name>
  <files>server/index.ts</files>
  <action>
    - Add endpoints to `server/index.ts`:
        - `GET /sessions`: List all sessions.
        - `POST /sessions`: Create a new session.
        - `GET /sessions/:id`: Get a specific session with its messages.
    - Implement basic data retrieval logic using Drizzle.
  </action>
  <verify>
    <automated>cd server && curl -X POST -H "Content-Type: application/json" -d '{"title": "Test Session"}' http://localhost:3000/sessions | grep "id"</automated>
  </verify>
  <done>Hono endpoints implemented for session management.</done>
</task>

</tasks>

<verification>
Verify that sessions can be created and retrieved from the SQLite database via the Hono API.
</verification>

<success_criteria>
- SQLite database `data.sqlite` is created.
- `POST /sessions` returns a 201 Created with a session ID.
- `GET /sessions` returns a list of sessions including the newly created one.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
