---
phase: 06-tool-use-external-knowledge
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [src/server/providers/types.ts, src/server/providers/ollama.ts, src/server/providers/openrouter.ts, src/server/orchestrator.ts, src/server/tools/implementations.ts]
autonomous: true
requirements: [TOOL-02, TOOL-03, KNOW-02, SEC-01]

must_haves:
  truths:
    - "Orchestrator can execute tools based on LLM requests"
    - "Document contents are injected into the reasoning context"
    - "File reading is restricted to the project root"
    - "Models correctly receive tool outputs and produce final answers"
  artifacts:
    - path: "src/server/tools/implementations.ts"
      provides: "read_file and calculator tools"
    - path: "src/server/orchestrator.ts"
      contains: "while (true) { ... tool execution logic ... }"
  key_links:
    - from: "src/server/orchestrator.ts"
      to: "src/server/tools/registry.ts"
      via: "Tool lookup and execution"
---

<objective>
Upgrade the orchestration loop to support tool use and implement initial core tools with security sandboxing.

Purpose: Enable personas to act on the system and reference uploaded documents.
Output: Tool-enabled orchestrator, sandboxed tools, and document context injection.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tool-use-external-knowledge/06-01-SUMMARY.md
@src/server/orchestrator.ts
@src/server/providers/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provider Tool Support</name>
  <files>src/server/providers/types.ts, src/server/providers/ollama.ts, src/server/providers/openrouter.ts</files>
  <action>
    1. Update `Message` in `types.ts` to include optional `tool_calls` and `tool_results`.
    2. Update `BaseProvider.generate` signature: `generate(modelId: string, messages: Message[], tools?: any[]): AsyncIterable<GenerateChunk>`.
    3. Define `GenerateChunk` to handle text OR tool calls.
    4. Implement tool call detection in `OllamaProvider` and `OpenRouterProvider`. 
       - For Ollama, use the `tools` parameter and check `response.message.tool_calls`.
       - For OpenRouter, pass `tools` in the POST body and parse tool calls from chunks.
  </action>
  <verify>
    <automated>ls src/server/providers/ollama.ts</automated>
  </verify>
  <done>Providers support tool definitions and can return tool calls.</done>
</task>

<task type="auto">
  <name>Task 2: Orchestrator Tool Loop & Document Injection</name>
  <files>src/server/orchestrator.ts</files>
  <action>
    1. Implement recursive tool loop in `runRoundTable`. If the LLM returns a tool call:
       - Execute the tool via `ToolRegistry`.
       - Add the tool result to the conversation history.
       - Re-call the LLM with the updated history until it produces a final text response.
    2. Before starting the persona loop, fetch all `documents` for the current `sessionId`.
    3. Concatenate document contents and inject them as a `system` message at the beginning of the context (Full Context Injection per D-02).
    4. Store tool call details in `messages.metadata` column.
  </action>
  <verify>
    <automated>grep -n "while" src/server/orchestrator.ts</automated>
  </verify>
  <done>Orchestrator handles tool calls and injects documents into the context.</done>
</task>

<task type="auto">
  <name>Task 3: Core Tools Implementation (Sandboxed)</name>
  <files>src/server/tools/implementations.ts</files>
  <action>
    1. Implement `read_file` tool:
       - Accepts `path`.
       - Per D-04: Strict sandboxing. Ensure path is within the project root using `path.resolve` and `startsWith`.
       - Returns file content as string.
    2. Implement `calculator` tool:
       - Accepts `expression`.
       - Safely evaluates (e.g., using `mathjs` or a simple parser, NOT `eval`).
    3. Register both tools in `ToolRegistry` on startup (initialize in `src/server/tools/implementations.ts`).
  </action>
  <verify>
    <automated>ls src/server/tools/implementations.ts</automated>
  </verify>
  <done>Initial tools implemented and registered with security constraints.</done>
</task>

</tasks>

<verification>
Ensure the orchestrator can complete a round where a persona uses the calculator tool or references data from an injected document.
</verification>

<success_criteria>
- `runRoundTable` supports recursive tool calls.
- `read_file` tool rejects paths outside project root.
- `calculator` tool correctly handles expressions.
- Documents are successfully injected into the system prompt.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tool-use-external-knowledge/06-02-SUMMARY.md`
</output>
