---
phase: 05-context-reporting
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified: [src/server/index.ts, src/components/DebateView.tsx, src/app/sessions/page.tsx]
autonomous: false
requirements: [API-02]

must_haves:
  truths:
    - "User can download session history as a JSON file"
    - "User can download session history as a Markdown file"
  artifacts:
    - path: "src/server/index.ts"
      provides: "Export endpoints"
    - path: "src/components/DebateView.tsx"
      provides: "Export UI buttons"
  key_links:
    - from: "src/components/DebateView.tsx"
      to: "/sessions/:id/export/*"
      via: "window.open or fetch"
      pattern: "/export"
---

<objective>
Provide session history export functionality in JSON and Markdown formats for external analysis and archiving.

Purpose: To allow users to use their round-table results outside the application.
Output: Export endpoints and UI controls.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/server/index.ts
@src/components/DebateView.tsx
@src/app/sessions/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement export endpoints</name>
  <files>src/server/index.ts</files>
  <action>
    Add GET endpoints `/sessions/:id/export/json` and `/sessions/:id/export/markdown` to `src/server/index.ts`.
    It should:
    1. Fetch session history for the given session.
    2. For JSON: Return raw messages as an array.
    3. For Markdown: Format the history as a human-readable transcript, including persona names and timestamps.
    4. Set appropriate headers for file download (`Content-Disposition: attachment`).
  </action>
  <verify>
    <automated>curl http://localhost:3001/sessions/[EXISTING_ID]/export/json</automated>
  </verify>
  <done>Export endpoints exist and correctly format session data for download.</done>
</task>

<task type="auto">
  <name>Task 2: UI for Export</name>
  <files>src/components/DebateView.tsx, src/app/sessions/page.tsx</files>
  <action>
    1. Add "Export JSON" and "Export Markdown" buttons to the `SessionPage` header or `DebateView` actions.
    2. Use `window.open` or a hidden link to trigger the file download from the new endpoints.
    3. Final polish: Ensure buttons look consistent with the theme.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>User can download discussion history directly from the browser.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Export functionality (JSON/Markdown)</what-built>
  <how-to-verify>
    1. Start a session with some debate history.
    2. Click the "Export JSON" button and verify the download.
    3. Click the "Export Markdown" button and verify the download.
    4. Check the content of the downloaded files for correctness and readability.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Ensure both JSON and Markdown export files are generated correctly and contain all the messages from the session history.
</verification>

<success_criteria>
1. Export endpoints correctly format data and provide downloads.
2. UI buttons trigger downloads as expected.
3. Exported Markdown is well-structured and easy to read.
</success_criteria>

<output>
After completion, create `.planning/phases/05-context-reporting/05-03-SUMMARY.md`
</output>
