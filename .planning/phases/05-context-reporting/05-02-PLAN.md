---
phase: 05-context-reporting
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [src/server/index.ts, src/components/DebateView.tsx, src/app/sessions/page.tsx]
autonomous: false
requirements: [UI-04]

must_haves:
  truths:
    - "User can trigger a final synthesis session audit report"
    - "Audit report is displayed in a clear, structured Markdown format"
  artifacts:
    - path: "src/server/index.ts"
      provides: "Synthesis endpoint"
    - path: "src/components/DebateView.tsx"
      provides: "UI for triggering and viewing the audit report"
  key_links:
    - from: "src/components/DebateView.tsx"
      to: "/sessions/:id/report"
      via: "fetch call"
      pattern: "/report"
---

<objective>
Generate a structured "Final Audit Report" synthesized by the Blue Hat persona to provide a comprehensive conclusion to the round-table discussion.

Purpose: To provide the user with a structured output for decision-making.
Output: Synthesis logic and UI components to trigger/view the report.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/server/index.ts
@src/components/DebateView.tsx
@src/app/sessions/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement synthesis endpoint</name>
  <files>src/server/index.ts</files>
  <action>
    Add a POST endpoint `/sessions/:id/report` to `src/server/index.ts`.
    It should:
    1. Fetch the entire message history for the session.
    2. Identify a "Blue Hat" persona (or use a default) with a synthesis-focused system prompt.
    3. Call the provider to generate a "Final Audit Report" summarizing:
       - Key facts (White Hat)
       - Major risks identified (Black Hat)
       - Top opportunities (Yellow Hat)
       - Creative suggestions (Green Hat)
       - Final decision/conclusion (Blue Hat).
    4. Save the resulting report as a 'system' or 'assistant' message in the database.
    5. Return the report content as JSON.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:3001/sessions/[EXISTING_ID]/report</automated>
  </verify>
  <done>Report endpoint exists and generates a structured synthesis message.</done>
</task>

<task type="auto">
  <name>Task 2: UI integration for Report Generation</name>
  <files>src/components/DebateView.tsx, src/app/sessions/page.tsx</files>
  <action>
    1. Add a "Generate Audit Report" button to the header or footer of the `DebateView` or `SessionPage`.
    2. Implement the frontend logic to call the report endpoint.
    3. Display the resulting report (which might contain Markdown) clearly in the UI.
    4. Consider using a library like `react-markdown` if not already present to render the report correctly.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>User can generate and view a Final Audit Report from the session UI.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Synthesis report generation flow</what-built>
  <how-to-verify>
    1. Start a session and add some messages.
    2. Click the "Generate Audit Report" button.
    3. Verify that a structured summary appears and is saved to the history.
    4. Confirm that the Markdown formatting is legible.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Ensure the synthesis report correctly summarizes all the "Six Hats" perspectives and is presented as a final structured document in the UI.
</verification>

<success_criteria>
1. Synthesis endpoint correctly uses the Blue Hat persona to review the discussion.
2. The UI allows triggering the report and displays it properly.
3. The report follows the structured audit format.
</success_criteria>

<output>
After completion, create `.planning/phases/05-context-reporting/05-02-SUMMARY.md`
</output>
