---
phase: 12-onboarding-agent
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified: [src/app/sessions/page.tsx, src/components/RefinementView.tsx, src/components/RefinementIndicator.tsx]
autonomous: false
requirements: [REQ-12.5]

must_haves:
  truths:
    - "User can engage in a Socratic dialogue with the Refiner"
    - "User can edit the refined prompt before confirming"
    - "Session UI transitions to the round-table view upon confirmation"
  artifacts:
    - path: "src/components/RefinementView.tsx"
      provides: "Main UI for the refinement dialogue"
    - path: "src/components/RefinementIndicator.tsx"
      provides: "Visual indication of the refinement phase"
    - path: "src/app/sessions/page.tsx"
      provides: "Updated session lifecycle logic"
  key_links:
    - from: "src/app/sessions/page.tsx"
      to: "src/components/RefinementView.tsx"
      via: "Conditional React rendering"
---

<objective>
Integrate the Onboarding Agent flow into the React frontend.

Purpose: Provide a guided user experience that transitions from initial prompt refinement to full round-table evaluation.
Output: Integrated refinement UI components and updated session page logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/sessions/page.tsx
@src/components/DebateView.tsx
@src/components/RoundTableConfig.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Refinement Components</name>
  <files>src/components/RefinementView.tsx, src/components/RefinementIndicator.tsx</files>
  <action>
    Develop the UI for the refinement phase.
    - RefinementView: A chat-like interface focusing on the dialogue with the Refiner.
    - Add an "Editable Refined Prompt" area where the user can tweak the agent's summary.
    - Add "Confirm & Start Audit" and "Skip & Start Audit" buttons.
    - RefinementIndicator: A simple status badge or banner indicating the current phase.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>UI components for refinement phase are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Session Phase Management</name>
  <files>src/app/sessions/page.tsx</files>
  <action>
    Update the main session page to handle the `refining` state.
    - Fetch the session and its status.
    - If `status === 'refining'`, render `RefinementView` instead of the standard `DebateView`.
    - Ensure new sessions start in the `refining` state.
    - Use the Hono streaming API (`/sessions/:id/refine`) for the dialogue.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Session page correctly manages the lifecycle between refinement and round-table.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Refinement Phase UI and Flow</what-built>
  <how-to-verify>
    1. Start a new session in the app.
    2. Verify the "Refiner" agent greets you and asks clarifying questions.
    3. Type some responses and see the Socratic dialogue in action.
    4. Click "Confirm & Start Audit" and verify the view switches to the round-table.
    5. Verify the round-table experts refer to the refined content.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Final Polishing & Verification</name>
  <files>src/app/sessions/page.tsx, tests/refinement.test.ts</files>
  <action>
    Finalize UI layout and run full integration tests.
    - Create `tests/refinement.test.ts` (if not done in Plan 02) for E2E flow.
    - Ensure smooth transitions and clear visual feedback during the refinement process.
    - Address any UI glitches or accessibility issues in the RefinementView.
  </action>
  <verify>
    <automated>npx vitest tests/refinement.test.ts</automated>
  </verify>
  <done>End-to-end flow verified and polished.</done>
</task>

</tasks>

<verification>
Perform the human verification checkpoint. Ensure all automated tests pass.
</verification>

<success_criteria>
- New sessions correctly prompt the user for refinement.
- Refinement UI is clear and intuitive.
- The transition to Round Table is seamless.
- The experts correctly incorporate the refined prompt.
</success_criteria>

<output>
After completion, create `.planning/phases/12-onboarding-agent/12-03-SUMMARY.md`
</output>
