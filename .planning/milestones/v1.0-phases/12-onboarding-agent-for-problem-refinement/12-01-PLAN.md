---
phase: 12-onboarding-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/server/db/schema.ts, src/server/db/seeds/onboarding.ts, src/server/services/refinement-service.ts]
autonomous: true
requirements: [REQ-12.1, REQ-12.2, REQ-12.3]

must_haves:
  truths:
    - "Sessions table contains 'status' and 'refinedPrompt' columns"
    - "A specialized 'Refiner' persona is available in the database"
    - "Initial sessions default to 'active' status to preserve legacy data"
  artifacts:
    - path: "src/server/db/schema.ts"
      provides: "Updated session schema"
    - path: "src/server/db/seeds/onboarding.ts"
      provides: "Seed for the Refiner persona"
    - path: "src/server/services/refinement-service.ts"
      provides: "Core refinement logic skeleton"
  key_links:
    - from: "src/server/db/seeds/onboarding.ts"
      to: "src/server/db/schema.ts"
      via: "Drizzle insert"
---

<objective>
Establish the persistence layer and seed data required for the Onboarding Agent. 

Purpose: Prepare the database to track session refinement states and provide the "Refiner" persona.
Output: Updated database schema, Refiner persona in DB, and a service skeleton for refinement logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/server/db/schema.ts
@src/server/db/seeds/personas.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Session Schema</name>
  <files>src/server/db/schema.ts</files>
  <action>
    Add 'status' and 'refinedPrompt' columns to the 'sessions' table.
    - status: text('status').notNull().default('active') (Options: 'refining', 'active', 'completed')
    - refinedPrompt: text('refined_prompt') (Nullable)
    
    Run 'npx drizzle-kit generate' and apply the migration if possible, or note the migration step for Wave 0.
  </action>
  <verify>
    <automated>npx drizzle-kit check</automated>
  </verify>
  <done>Session table schema updated with new columns.</done>
</task>

<task type="auto">
  <name>Task 2: Seed Refiner Persona</name>
  <files>src/server/db/seeds/onboarding.ts</files>
  <action>
    Create a new seed file for the Refiner persona.
    - Name: "Refiner"
    - Role: "Facilitator"
    - System Prompt: Socratic dialogue prompt focused on clarifying goals and constraints.
    - Icon: "bot" or "user-circle"
    - Color: "indigo"
    
    Implement a `seedRefiner` function similar to `seedThinkingHats` in `src/server/db/seeds/personas.ts`.
  </action>
  <verify>
    <automated>ls src/server/db/seeds/onboarding.ts</automated>
  </verify>
  <done>Refiner persona seed file created and ready for execution.</done>
</task>

<task type="auto">
  <name>Task 3: Refinement Service Foundation</name>
  <files>src/server/services/refinement-service.ts</files>
  <action>
    Implement a skeleton for RefinementService.
    - define `RefinementStatus` enum/type.
    - define `confirmRefinement(sessionId: string, finalPrompt: string)` function.
    - define `skipRefinement(sessionId: string)` function.
    - Add TODO for the `runRefinement` generator which will be implemented in Plan 02.
  </action>
  <verify>
    <automated>npx tsc src/server/services/refinement-service.ts --noEmit</automated>
  </verify>
  <done>Service skeleton implemented with core state transition methods.</done>
</task>

</tasks>

<verification>
Verify that the database migrations can be generated and that the seed file is correctly structured.
</verification>

<success_criteria>
- sessions table has 'status' and 'refined_prompt' columns.
- src/server/db/seeds/onboarding.ts exports a valid seed function.
- src/server/services/refinement-service.ts compiles without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/12-onboarding-agent/12-01-SUMMARY.md`
</output>
