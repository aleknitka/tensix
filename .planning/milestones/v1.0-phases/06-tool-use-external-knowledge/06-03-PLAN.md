---
phase: 06-tool-use-external-knowledge
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: [src/components/DocumentManager.tsx, src/components/DebateView.tsx, src/app/sessions/page.tsx]
autonomous: false
requirements: [KNOW-01, UI-05]

must_haves:
  truths:
    - "User can upload a text file to the session"
    - "Tool execution is clearly visible in the debate flow"
    - "User can see which files are currently in the knowledge base"
  artifacts:
    - path: "src/components/DocumentManager.tsx"
      provides: "File upload and list UI"
    - path: "src/components/DebateView.tsx"
      contains: "metadata"
  key_links:
    - from: "src/components/DocumentManager.tsx"
      to: "/api/sessions/:id/documents"
      via: "POST/GET requests"
---

<objective>
Expose tool execution and document management in the user interface.

Purpose: Provide transparency into tool use and enable users to add custom context to sessions.
Output: Document management UI and tool-aware debate visualization.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tool-use-external-knowledge/06-02-SUMMARY.md
@src/components/DebateView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Management UI</name>
  <files>src/components/DocumentManager.tsx, src/app/sessions/page.tsx</files>
  <action>
    1. Create `src/components/DocumentManager.tsx`:
       - A component that lists documents for the current session.
       - A "Upload" button that takes a file, reads its content, and POSTs to `/api/sessions/:id/documents`.
       - A "Delete" button for each document.
    2. Add the `DocumentManager` to the session page (likely in a side drawer or the top area).
  </action>
  <verify>
    <automated>ls src/components/DocumentManager.tsx</automated>
  </verify>
  <done>Users can upload and manage documents within a session.</done>
</task>

<task type="auto">
  <name>Task 2: Tool Execution Visibility</name>
  <files>src/components/DebateView.tsx</files>
  <action>
    1. Update `DebateView` to check for `metadata` in messages.
    2. If `metadata` contains tool call information:
       - Render a specialized "Tool Execution" card.
       - Show the tool name, arguments, and status (running/done).
       - Per D-03: Use distinctive styling to separate tool calls from persona reasoning.
  </action>
  <verify>
    <automated>grep -n "metadata" src/components/DebateView.tsx</automated>
  </verify>
  <done>Tool executions are visible to the user during a debate.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Full end-to-end tool use and document integration.</what-built>
  <how-to-verify>
    1. Create a session.
    2. Upload a text file (e.g., "secret_code.txt" containing "The secret code is 12345").
    3. Run a round with a persona (e.g., White Hat) and ask it to find the secret code or use the calculator.
    4. Confirm that:
       - The persona identifies the secret code from the uploaded file.
       - If the calculator is used, a "Tool Execution" card appears in the UI.
       - The persona provides a correct final answer incorporating the tool result.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Verify visual consistency of the document manager and the tool execution cards in the UI.
</verification>

<success_criteria>
- User can successfully upload a file and see it listed.
- Round-table reasoning reflects content from the uploaded file.
- Tool calls generate visible cards in the chat flow.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tool-use-external-knowledge/06-03-SUMMARY.md`
</output>
