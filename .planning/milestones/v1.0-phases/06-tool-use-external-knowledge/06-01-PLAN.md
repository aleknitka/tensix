---
phase: 06-tool-use-external-knowledge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/server/db/schema.ts, src/server/tools/registry.ts, src/server/index.ts]
autonomous: true
requirements: [TOOL-01, KNOW-01]

must_haves:
  truths:
    - "Database schema supports document storage per session"
    - "Messages can store arbitrary metadata for tool calls"
    - "System has a centralized registry for defining tool schemas"
  artifacts:
    - path: "src/server/tools/registry.ts"
      provides: "Tool definition and lookup"
    - path: "src/server/db/schema.ts"
      contains: "documents = sqliteTable"
  key_links:
    - from: "src/server/index.ts"
      to: "src/server/db/schema.ts"
      via: "Hono API endpoints for documents"
---

<objective>
Establish the foundational data structures and registry for tool use and document-based knowledge.

Purpose: Enable persistence for uploaded files and structured metadata for tool execution events.
Output: Updated database schema, tool registry system, and document management API.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/server/db/schema.ts
@src/server/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Database Schema</name>
  <files>src/server/db/schema.ts</files>
  <action>
    1. Add a `documents` table with:
       - `id`: primary key (text)
       - `sessionId`: reference to sessions.id (cascade delete)
       - `name`: file name (text)
       - `content`: file content (text)
       - `type`: MIME type or extension (text)
       - `createdAt`: timestamp
    2. Add a `metadata` column to the `messages` table as `text('metadata')`. We will use this to store JSON tool call details.
    3. Run `npx drizzle-kit push` to apply changes to `sqlite.db`.
  </action>
  <verify>
    <automated>npx drizzle-kit check</automated>
  </verify>
  <done>Schema updated with documents table and messages.metadata column.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ToolRegistry Foundation</name>
  <files>src/server/tools/registry.ts</files>
  <action>
    1. Create `src/server/tools/registry.ts`.
    2. Define `Tool` interface:
       ```typescript
       export interface Tool {
         name: string;
         description: string;
         parameters: any; // JSON Schema
         execute: (args: any) => Promise<string>;
       }
       ```
    3. Implement `ToolRegistry` class with:
       - `register(tool: Tool): void`
       - `getTool(name: string): Tool | undefined`
       - `getAllTools(): Tool[]`
       - `getToolDefinitions(): any[]` (Formatted for Ollama/OpenRouter tools array)
  </action>
  <verify>
    <automated>ls src/server/tools/registry.ts</automated>
  </verify>
  <done>ToolRegistry implemented and ready for tool registration.</done>
</task>

<task type="auto">
  <name>Task 3: Document CRUD API</name>
  <files>src/server/index.ts</files>
  <action>
    1. Add Hono routes for document management:
       - `POST /api/sessions/:id/documents`: Accepts { name, content, type }, saves to DB.
       - `GET /api/sessions/:id/documents`: Returns list of documents for a session.
       - `DELETE /api/documents/:id`: Removes a document.
    2. Per D-02 in 06-CONTEXT.md: Ensure these routes use the `documents` table created in Task 1.
  </action>
  <verify>
    <automated>curl -X GET http://localhost:3000/api/sessions/test/documents</automated>
  </verify>
  <done>API endpoints for document management are functional.</done>
</task>

</tasks>

<verification>
Check that the database reflects the new schema and the registry correctly stores/retrieves tool definitions.
</verification>

<success_criteria>
- `documents` table exists in SQLite.
- `messages` table has `metadata` column.
- ToolRegistry can return JSON schema definitions for registered tools.
- API endpoints for documents return successful responses.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tool-use-external-knowledge/06-01-SUMMARY.md`
</output>
