---
phase: 08-markdown-rendering-in-chat
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [src/components/Markdown.tsx, src/app/globals.css, tests/markdown.test.tsx]
autonomous: true
requirements: [REND-01, REND-03]

must_haves:
  truths:
    - "Markdown renders formatted text correctly (headers, tables, lists)"
    - "Code blocks feature syntax highlighting"
    - "Copy button is functional and provides visual feedback"
    - "Streaming tables remain stable (no drastic shifts)"
  artifacts:
    - path: "src/components/Markdown.tsx"
      provides: "Reusable Markdown renderer component"
    - path: "tests/markdown.test.tsx"
      provides: "Unit tests for Markdown component"
  key_links:
    - from: "src/components/Markdown.tsx"
      to: "react-markdown"
      via: "core renderer"
    - from: "src/components/Markdown.tsx"
      to: "react-syntax-highlighter"
      via: "code block rendering"
---

<objective>
Implement the custom Markdown component with code highlighting, copy-to-clipboard functionality, and stable streaming table layout.

Purpose: Create the core rendering engine for chat messages that handles rich formatting and interactive code elements.
Output: Markdown component with Prism syntax highlighting, GFM support, and comprehensive tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-markdown-rendering-in-chat/08-CONTEXT.md
@.planning/phases/08-markdown-rendering-in-chat/08-RESEARCH.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the Markdown component</name>
  <files>src/components/Markdown.tsx</files>
  <action>
    - Create `src/components/Markdown.tsx`.
    - Use `react-markdown` (v10) as the core.
    - Add `remark-gfm` to `remarkPlugins`.
    - Create a custom `CodeBlock` component (as specified in 08-RESEARCH.md) to override the default `code` component:
      - Use `react-syntax-highlighter` (Prism) with `vscDarkPlus` theme.
      - Implement a "Copy" button that appears on hover.
      - Use the Clipboard API for copying and `useState` for visual feedback (e.g., "Copied!").
    - Apply `prose prose-zinc dark:prose-invert max-w-none` to the root container.
    - Conditionally apply `prose-invert` if a `variant="user"` prop is passed.
    - Ensure links open in a new tab (`target="_blank"`).
  </action>
  <verify>
    <automated>grep -q "react-markdown" src/components/Markdown.tsx && grep -q "react-syntax-highlighter" src/components/Markdown.tsx</automated>
  </verify>
  <done>Markdown component is implemented with Prism and GFM support.</done>
</task>

<task type="auto">
  <name>Task 2: Implement stable streaming table styles</name>
  <files>src/app/globals.css</files>
  <action>
    - Add CSS classes to `src/app/globals.css` to stabilize table rendering during streaming:
      - `.prose table { table-layout: fixed; width: 100%; }`
      - `.prose td, .prose th { min-width: 120px; overflow-wrap: break-word; }`
  </action>
  <verify>
    <automated>grep -q "table-layout: fixed" src/app/globals.css</automated>
  </verify>
  <done>Table styles are configured to prevent layout shifts.</done>
</task>

<task type="auto">
  <name>Task 3: Create unit tests for Markdown component</name>
  <files>tests/markdown.test.tsx</files>
  <action>
    - Create `tests/markdown.test.tsx`.
    - Write tests using Vitest and React Testing Library to verify:
      - Correct rendering of headers, lists, and bold text.
      - Table rendering (GFM).
      - Code block presence and language detection.
      - "Copy" button existence and behavior (mocking `navigator.clipboard`).
  </action>
  <verify>
    <automated>npm run test -- tests/markdown.test.tsx</automated>
  </verify>
  <done>Unit tests cover core Markdown features and custom components.</done>
</task>

</tasks>

<verification>
Run `npm run test` to ensure the component behaves as expected.
</verification>

<success_criteria>
- Markdown rendering works for all GFM elements.
- Code syntax highlighting is active.
- Copy button works and provides feedback.
- Tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/08-markdown-rendering-in-chat/08-02-SUMMARY.md`
</output>
