---
phase: 05-context-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/server/orchestrator.ts]
autonomous: true
requirements: [ORCH-03]

must_haves:
  truths:
    - "System automatically reduces context size when session length increases"
    - "Summaries are generated and used in subsequent persona turns"
  artifacts:
    - path: "src/server/orchestrator.ts"
      provides: "Summarization logic and context management"
  key_links:
    - from: "src/server/orchestrator.ts"
      to: "LLM Provider"
      via: "summarizeMessages call"
      pattern: "summarizeMessages"
---

<objective>
Implement automated context management in the SEQ orchestrator via periodic summarization to prevent context window overflow.

Purpose: To allow long-running round-table sessions without exceeding LLM limits.
Output: Context-aware orchestrator with a summarization trigger.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/server/orchestrator.ts
@src/server/db/schema.ts
@src/server/db/seeds/personas.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement summarizeMessages function</name>
  <files>src/server/orchestrator.ts</files>
  <behavior>
    - Given a list of messages, the function should use a "Blue Hat" or default persona to produce a concise summary.
    - The summary should encapsulate the core points of the discussion so far.
    - Test that it calls the provider's `generate` method with a summarization-focused prompt.
  </behavior>
  <action>
    Create a `summarizeMessages` function in `src/server/orchestrator.ts`.
    It should:
    1. Identify a "Blue Hat" persona from the database (fallback to a hardcoded prompt if none exist).
    2. Construct a prompt asking to summarize the conversation into a few sentences.
    3. Use the first available provider/model to generate this summary.
    4. Return the summary string.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create src/server/__tests__/orchestrator.test.ts first</automated>
  </verify>
  <done>Summarization function exists and returns a summary string from the LLM.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate summarization trigger into runRoundTable</name>
  <files>src/server/orchestrator.ts</files>
  <action>
    Modify `runRoundTable` in `src/server/orchestrator.ts`:
    1. Define a context threshold (e.g., `MAX_MESSAGES = 10`).
    2. Before processing personas, check if `baseMessages.length > MAX_MESSAGES`.
    3. If threshold exceeded, call `summarizeMessages`.
    4. Replace the old messages in `baseMessages` with a single summary message (role: 'system' or 'assistant').
    5. Save the summary to the database as a "System" or "Summarizer" message so it's persistent and visible in history.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Long sessions are automatically summarized before the next round-table evaluation starts.</done>
</task>

</tasks>

<verification>
Ensure that sessions with more than 10 messages trigger a summarization step that condenses the history into a single message for the next agent's context window.
</verification>

<success_criteria>
1. `summarizeMessages` correctly interacts with the LLM.
2. `runRoundTable` detects long history and triggers summarization.
3. Subsequent agents receive the summary instead of the full raw history.
</success_criteria>

<output>
After completion, create `.planning/phases/05-context-reporting/05-01-SUMMARY.md`
</output>
