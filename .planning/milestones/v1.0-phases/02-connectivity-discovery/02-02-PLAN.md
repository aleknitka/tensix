---
phase: 02-connectivity-discovery
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/app/settings/page.tsx, src/components/Sidebar.tsx, src/components/ProviderConfigForm.tsx, src/components/ModelSelector.tsx]
autonomous: false
requirements: [CONN-01, CONN-02, CONN-03, CONN-04]

must_haves:
  truths:
    - "User can navigate to a Settings page from the Sidebar."
    - "Settings page allows configuring Ollama URL, LM Studio URL, and OpenRouter API key."
    - "System shows real-time health status for each provider (Connected/Error)."
    - "User can view a list of available models with their detected capabilities."
  artifacts:
    - path: "src/app/settings/page.tsx"
      provides: "Main settings view"
    - path: "src/components/ProviderConfigForm.tsx"
      provides: "UI for managing provider connections"
    - path: "src/components/ModelSelector.tsx"
      provides: "UI for viewing/selecting models"
  key_links:
    - from: "src/app/settings/page.tsx"
      to: "http://localhost:3001/providers"
      via: "fetch"
---

<objective>
Implement the frontend for managing LLM provider connections and discovering models.

Purpose: Allow users to configure, verify, and select models from local and cloud providers.
Output: A Settings page with real-time feedback and model discovery indicators.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-connectivity-discovery/02-01-SUMMARY.md
@src/components/Sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Settings Page and Navigation</name>
  <files>src/app/settings/page.tsx, src/components/Sidebar.tsx</files>
  <action>
    - Add a `Settings` button at the bottom of the `Sidebar.tsx`, using the `Settings` icon from `lucide-react`.
    - Create a basic Settings page layout in `src/app/settings/page.tsx` that links to the Sidebar.
  </action>
  <verify>
    <automated>test -f src/app/settings/page.tsx && grep -i "settings" src/components/Sidebar.tsx</automated>
  </verify>
  <done>Navigation to the Settings page is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Provider Configuration UI with Health Feedback</name>
  <files>src/app/settings/page.tsx, src/components/ProviderConfigForm.tsx</files>
  <action>
    - Build `ProviderConfigForm.tsx` to handle URLs and API keys for Ollama, LM Studio, and OpenRouter.
    - Implement real-time connection status using visual cues (e.g., green dot for Connected, red for Error).
    - Provide descriptive error messages if the backend health check fails.
  </action>
  <verify>
    <automated>grep -i "health" src/components/ProviderConfigForm.tsx</automated>
  </verify>
  <done>Settings UI provides immediate feedback on provider connection status.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Settings UI for provider configuration and health check feedback.</what-built>
  <how-to-verify>
    1. Run the application and navigate to Settings.
    2. Enter a valid Ollama/LM Studio URL or OpenRouter key.
    3. Verify that the "Connected" indicator appears.
    4. Enter an invalid URL and verify that the "Error" status and message are shown.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Dynamic Model Listing & Capability Indicators</name>
  <files>src/components/ModelSelector.tsx, src/app/settings/page.tsx</files>
  <action>
    - Create `ModelSelector.tsx` to display the list of available models from the backend.
    - Add capability indicators (e.g., "Supports System Prompt", "Vision Ready") based on the capability detection logic from the backend.
    - Ensure the list updates when provider configurations are changed or refreshed.
  </action>
  <verify>
    <automated>grep -i "capabilities" src/components/ModelSelector.tsx</automated>
  </verify>
  <done>Model discovery and capability information are visible in the Settings UI.</done>
</task>

</tasks>

<verification>
Perform a full check of the settings workflow, ensuring connections are persistent and models are correctly detected.
</verification>

<success_criteria>
1. User can configure and verify provider connections from the Settings UI.
2. Dynamically updated model list reflects available models on local/cloud backends.
3. Capabilities of each model are clearly indicated to the user.
</success_criteria>

<output>
After completion, create `.planning/phases/02-connectivity-discovery/02-02-SUMMARY.md`
</output>
