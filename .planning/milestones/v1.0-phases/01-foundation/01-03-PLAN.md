---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/components/Sidebar.tsx
  - src/components/StatusIndicator.tsx
autonomous: false
requirements: [UI-03]
must_haves:
  truths:
    - "User can see and interact with the session history sidebar"
    - "Backend status and app version are visible in the UI"
  artifacts:
    - path: "src/components/Sidebar.tsx"
      provides: "Session navigation UI"
    - path: "src/components/StatusIndicator.tsx"
      provides: "System status display"
  key_links:
    - from: "src/components/Sidebar.tsx"
      to: "http://localhost:3000/sessions"
      via: "API fetch"
---

<objective>
Build the core UI components: session history sidebar and status indicators.
Purpose: Satisfy the user-facing success criteria of Phase 1.
Output: A functional sidebar for session navigation and a status display for app/backend.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Application Layout and Sidebar</name>
  <files>src/app/layout.tsx, src/components/Sidebar.tsx</files>
  <action>
    - Update the root `src/app/layout.tsx` to include a side-by-side layout (Sidebar + Main content).
    - Create `src/components/Sidebar.tsx` to fetch and display the session history from the Hono API (`GET /sessions`).
    - Styling: Use Tailwind CSS for a professional, dark-themed Sidebar with a "New Session" button.
  </action>
  <verify>
    <automated>grep -q "Sidebar" src/app/layout.tsx && [ -f src/components/Sidebar.tsx ]</automated>
  </verify>
  <done>Core layout and session sidebar implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Session Selection and Creation</name>
  <files>src/components/Sidebar.tsx, src/app/page.tsx</files>
  <action>
    - Add logic to the "New Session" button in the sidebar to `POST /sessions`.
    - Implement session selection: clicking a session in the list should update the main view (initially just displaying the session ID or title).
    - Use React state/context or a simple URL param (`/?sessionId=...`) to track the current session.
  </action>
  <verify>
    <automated>grep -q "POST" src/components/Sidebar.tsx && grep -q "sessionId" src/app/page.tsx</automated>
  </verify>
  <done>Interactivity for creating and switching between sessions added.</done>
</task>

<task type="auto">
  <name>Task 3: Add Status Indicators</name>
  <files>src/components/StatusIndicator.tsx, src/app/page.tsx</files>
  <action>
    - Create `src/components/StatusIndicator.tsx` to display:
        - App Version: "v0.1.0" (hardcoded for now).
        - Backend Status: "Connected" or "Disconnected" (polled from `GET /health`).
    - Place the status indicators in the bottom of the sidebar or top header.
  </action>
  <verify>
    <automated>[ -f src/components/StatusIndicator.tsx ] && grep -q "StatusIndicator" src/app/page.tsx</automated>
  </verify>
  <done>Application version and backend status visible in the UI.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Final Foundation Verification</name>
  <action>Verify the full vertical slice: Next.js + Tauri + Hono + SQLite + Sidebar.</action>
  <verify>
    <automated># Manual check required</automated>
  </verify>
  <done>Human approves the foundation and sidebar functionality.</done>
</task>

</tasks>

<verification>
Full vertical slice verification from UI through API to SQLite.
</verification>

<success_criteria>
- User can launch the app and see the sidebar with session history.
- Sessions can be created and persist across app reloads (stored in SQLite).
- App version and backend connection status are clearly visible.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
