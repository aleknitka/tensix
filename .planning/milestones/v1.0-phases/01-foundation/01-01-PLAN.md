---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src-tauri/tauri.conf.json
  - server/package.json
  - server/index.ts
autonomous: true
requirements: [API-01]
must_haves:
  truths:
    - "Application launches in a Tauri window"
    - "Frontend can communicate with Hono backend health endpoint"
  artifacts:
    - path: "src-tauri/tauri.conf.json"
      provides: "Tauri v2 configuration"
    - path: "server/index.ts"
      provides: "Hono API entry point"
  key_links:
    - from: "src/app/page.tsx"
      to: "http://localhost:3000/health"
      via: "fetch"
---

<objective>
Initialize the project scaffold with Next.js 14 (App Router), Tauri v2, and Hono (on Bun).
Purpose: Establish the base technical stack for the desktop application and its local API.
Output: A working Tauri window running a Next.js frontend that can talk to a Hono backend.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js and Tauri v2</name>
  <files>package.json, src-tauri/tauri.conf.json, src/app/page.tsx</files>
  <action>
    - Initialize a Next.js 14 project at the root using Bun: `bun create next-app . --tailwind --app --src-dir --import-alias "@/*" --use-bun`.
    - Install Tauri CLI: `bun add -D @tauri-apps/cli@next`.
    - Initialize Tauri v2: `bun tauri init`. Configure it to use `bun run dev` for development and `bun run build` for production.
    - Set the identifier to `com.tensix.app`.
    - Update `src/app/page.tsx` with a basic "Tensix Foundation" landing message.
  </action>
  <verify>
    <automated>bun tauri info && [ -f src-tauri/tauri.conf.json ]</automated>
  </verify>
  <done>Next.js project initialized and Tauri v2 wrapper configured.</done>
</task>

<task type="auto">
  <name>Task 2: Setup Hono Backend on Bun</name>
  <files>server/package.json, server/index.ts</files>
  <action>
    - Create a `server` directory at the root.
    - Initialize a Bun project in `server`: `cd server && bun init -y`.
    - Add Hono: `bun add hono`.
    - Create `server/index.ts` with a basic Hono app that has a `GET /health` endpoint returning `{"status": "ok", "version": "0.1.0"}`.
    - Add a "dev" script to `server/package.json`: `"dev": "bun --watch index.ts"`.
  </action>
  <verify>
    <automated>cd server && bun index.ts & sleep 1 && curl http://localhost:3000/health && pkill -f "bun index.ts"</automated>
  </verify>
  <done>Hono backend initialized with a health check endpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Establish API Communication</name>
  <files>src/app/page.tsx</files>
  <action>
    - Update the Next.js home page (`src/app/page.tsx`) to fetch from `http://localhost:3000/health` on mount and display the status.
    - Ensure CORS is handled in Hono (`server/index.ts`) if necessary, although for local development it might be simpler to allow all or match the Tauri origin.
  </action>
  <verify>
    <automated># Manual verification in Tauri window will be done in next plan, here we check for fetch call in code
grep -q "fetch(.*localhost:3000/health" src/app/page.tsx</automated>
  </verify>
  <done>Frontend updated to communicate with the backend API.</done>
</task>

</tasks>

<verification>
Check that the project can start its dev environment and the frontend can reach the backend health endpoint.
</verification>

<success_criteria>
- Tauri window opens showing the Next.js app.
- Next.js app displays "Backend Status: Connected (v0.1.0)" based on the Hono response.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
