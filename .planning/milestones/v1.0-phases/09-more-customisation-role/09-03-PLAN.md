---
phase: 09-more-customisation-role
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified: [src/components/PersonaEditor.tsx, src/components/PromptTemplateLibrary.tsx, src/components/DebateView.tsx]
autonomous: true
requirements: [UI-09, UI-10]
must_haves:
  truths:
    - "Sandbox area allows testing persona behavior with real-time streaming"
    - "Sandbox is stateless and does not affect session history"
    - "Prompt templates append reasoning patterns to the system prompt"
    - "Chat interface displays persona-specific icons and color accents"
  artifacts:
    - path: "src/components/PromptTemplateLibrary.tsx"
      provides: "Library of Reasoning Patterns"
    - path: "src/components/DebateView.tsx"
      provides: "Branding-aware message rendering"
  key_links:
    - from: "src/components/PersonaEditor.tsx"
      to: "POST /personas/test"
      via: "sandbox execution"
      pattern: "fetch.*/personas/test"
    - from: "src/components/DebateView.tsx"
      to: "Persona Metadata"
      via: "props or context"
      pattern: "icon_id|color_accent"
---

<objective>
Finalize the role customization experience with live testing and a library of reasoning patterns. Update chat UI to reflect persona branding.

Purpose: Provide a "try-before-you-buy" experience for persona editing and common prompt engineering templates.
Output: Integrated Sandbox and Template Library in the editor.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-more-customisation-role/09-CONTEXT.md
@.planning/phases/09-more-customisation-role/09-RESEARCH.md
@.planning/phases/09-more-customisation-role/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live Sandbox Area</name>
  <files>src/components/PersonaEditor.tsx</files>
  <action>
    Add a "Sandbox" area to `PersonaEditor.tsx` (per D-02):
    - Input for a test prompt.
    - Model selector for testing.
    - "Test" button that calls the `/personas/test` endpoint using the current (unsaved) editor state.
    - Result area that displays the streamed response.
    - Support for cancellation during generation.
  </action>
  <verify>
    <automated>grep -E "Sandbox|Test|personas/test" src/components/PersonaEditor.tsx</automated>
  </verify>
  <done>Working live sandbox integrated into the editor.</done>
</task>

<task type="auto">
  <name>Task 2: Prompt Template Library</name>
  <files>src/components/PromptTemplateLibrary.tsx, src/components/PersonaEditor.tsx</files>
  <action>
    Implement reasoning patterns (per D-05):
    - Create a dropdown or modal containing patterns: "Chain of Thought", "Socratic Method", "Direct & Concise", "Critique & Refine".
    - When selected, append the corresponding suffix (from RESEARCH.md) to the `systemPrompt` field.
    - Integrate this into the System Prompt section of the `PersonaEditor`.
  </action>
  <verify>
    <automated>grep -E "Chain of Thought|Socratic Method" src/components/PersonaEditor.tsx src/components/PromptTemplateLibrary.tsx</automated>
  </verify>
  <done>Prompt template library implemented and inserting templates.</done>
</task>

<task type="auto">
  <name>Task 3: Apply Branding to Chat UI</name>
  <files>src/components/DebateView.tsx</files>
  <action>
    Update message rendering in `src/components/DebateView.tsx` (or the relevant message component):
    - Use `icon_id` and `color_accent` from the persona metadata.
    - Style the avatar with the selected icon and accent color.
    - Consider adding a subtle border or background accent to messages based on the `color_accent`.
  </action>
  <verify>
    <automated>grep -E "icon_id|color_accent" src/components/DebateView.tsx</automated>
  </verify>
  <done>Persona branding is visible in the chat interface.</done>
</task>

</tasks>

<verification>
- Sandbox successfully streams response from chosen model using local editor state.
- Selecting a prompt template appends the correct text to the system prompt.
- Branding (icon/color) is reflected in the chat display.
</verification>

<success_criteria>
- The editor includes a working sandbox for immediate persona feedback.
- Reasoning templates can be easily inserted into system prompts.
- Users can visually identify personas in the chat via icons and colors.
</success_criteria>

<output>
After completion, create `.planning/phases/09-more-customisation-role/09-03-SUMMARY.md`
</output>
