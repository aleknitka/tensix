---
phase: 04-persona-management-moderation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/server/index.ts, src/app/settings/personas/page.tsx, src/components/PersonaEditor.tsx]
autonomous: true
requirements: [PERS-02]

must_haves:
  truths:
    - "User can view a list of all existing personas (including pre-built hats)"
    - "User can create a new custom persona with a specific system prompt and model"
    - "User can edit or delete their custom personas"
    - "Newly created personas appear in the Round Table configuration list for selection"
  artifacts:
    - path: "src/server/index.ts"
      provides: "Persona CRUD API endpoints"
      exports: ["GET /personas", "POST /personas", "PUT /personas/:id", "DELETE /personas/:id"]
    - path: "src/app/settings/personas/page.tsx"
      provides: "Persona management list and UI"
    - path: "src/components/PersonaEditor.tsx"
      provides: "Form component for creating/editing personas"
  key_links:
    - from: "src/app/settings/personas/page.tsx"
      to: "/personas"
      via: "fetch"
      pattern: "fetch.*personas"
---

<objective>
Enable custom persona management (PERS-02). Users should be able to create, edit, and save their own personas with specific system prompts and assigned models, rather than being limited to the pre-built Six Thinking Hats.

Purpose: Provide flexibility for different evaluation frameworks and user-specific needs.
Output: Backend API for persona CRUD and a Management UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/server/db/schema.ts
@src/server/index.ts
@src/components/RoundTableConfig.tsx

<interfaces>
From src/server/db/schema.ts:
```typescript
export const personas = sqliteTable('personas', {
  id: text('id').primaryKey(),
  name: text('name').notNull(),
  role: text('role').notNull(),
  systemPrompt: text('system_prompt').notNull(),
  modelId: text('model_id').notNull(),
  providerId: text('provider_id').references(() => providers.id),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Persona CRUD API</name>
  <files>src/server/index.ts</files>
  <action>
    Add the following Hono routes to `src/server/index.ts`:
    - `POST /personas`: Accept { name, role, systemPrompt, modelId, providerId }, generate UUID, and insert into DB.
    - `PUT /personas/:id`: Accept the same fields (partial or full) and update the persona by ID.
    - `DELETE /personas/:id`: Remove the persona from the database by ID.
    
    Ensure that `GET /personas` still works as before (listing all).
  </action>
  <verify>
    <automated>
      curl -X POST http://localhost:3001/personas -H "Content-Type: application/json" -d '{"name":"Test Persona","role":"Tester","systemPrompt":"You are a tester.","modelId":"llama3","providerId":"ollama-1"}'
      # Then check if it exists:
      curl http://localhost:3001/personas | grep "Test Persona"
    </automated>
  </verify>
  <done>Backend API supports full CRUD for personas.</done>
</task>

<task type="auto">
  <name>Task 2: Create Persona Management UI</name>
  <files>src/app/settings/personas/page.tsx, src/components/PersonaEditor.tsx</files>
  <action>
    - Create `src/components/PersonaEditor.tsx`: A form with inputs for name, role, system prompt, and a model selector (using `GET /models`).
    - Create `src/app/settings/personas/page.tsx`: 
      - List existing personas with "Edit" and "Delete" buttons.
      - A "Create Persona" button that opens the editor (either as a modal or a toggle).
      - Connect both to the new API endpoints.
    - Add a link to this page in the Sidebar or Settings view.
  </action>
  <verify>
    <automated>MISSING — Manual visual check needed (checkpoint:human-verify)</automated>
  </verify>
  <done>User can create and manage personas via the UI.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Persona CRUD API and Management UI</what-built>
  <how-to-verify>
    1. Visit the Persona Management page.
    2. Create a new persona named "Cynic" with a negative prompt.
    3. Verify it appears in the list.
    4. Edit the "Cynic" to change its model or prompt.
    5. Go to a session and verify "Cynic" appears in the Round Table Configuration list.
    6. Delete "Cynic" and verify it's gone from both lists.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Check that `curl http://localhost:3001/personas` returns the updated list after a creation.
Check that the Next.js page renders without errors and connects to the backend.
</verification>

<success_criteria>
1. User can successfully create a custom persona.
2. User can successfully edit an existing custom persona.
3. User can successfully delete a custom persona.
4. Custom personas are usable in a Round Table session.
</success_criteria>

<output>
After completion, create `.planning/phases/04-persona-management-moderation/04-01-SUMMARY.md`
</output>
